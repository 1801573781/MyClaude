# 项目函数级摘要 for D:\AI\MyClaude

# 生成时间：2026-08-03 07:03:45

## 文件：src/A2A/myorch/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/A2A/myorch/agent_card.py

> 文件概述：定义验证编排服务的A2A身份与能力，暴露验证、查询和单元测试委派三项技能。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `get_agent_card` | L41-L66 | 构建并返回 MyOrch 验证编排服务的 AgentCard 对象 |

---
## 文件：src/A2A/myorch/context_store.py

> 文件概述：持久化进化验证任务的元数据与报告，支持按任务ID查询历史记录。

### 类列表

#### 类：`ContextStore` (L25-L283)

> 类概述：验证任务上下文持久化存储，基于JSON文件管理任务生命周期。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L32-L39 | 初始化存储根目录并创建必要文件夹。 |
| `_task_dir` | L45-L49 | 获取并创建指定任务ID的目录路径。 |
| `_write_json` | L52-L57 | 通过临时文件实现原子化写入JSON。 |
| `_read_json` | L60-L65 | 读取JSON文件，不存在时返回None。 |
| `create_task` | L71-L105 | 创建验证任务记录，保存元数据与测试用例。 |
| `get_task` | L107-L127 | 根据任务ID查询验证任务对象。 |
| `get_request` | L129-L156 | 获取任务的原始验证请求数据。 |
| `update_status` | L158-L171 | 更新任务状态并记录更新时间。 |
| `update_progress` | L173-L186 | 更新任务进度信息并记录更新时间。 |
| `save_report` | L188-L196 | 将验证报告持久化保存到JSON文件。 |
| `get_report` | L198-L211 | 读取并返回任务的验证报告。 |
| `list_tasks` | L213-L226 | 列出所有包含元数据文件的任务ID。 |
| `get_metrics` | L232-L283 | 统计全部任务的通过率与平均执行时间等指标。 |

---
## 文件：src/A2A/myorch/main.py

> 文件概述：A2A 验证编排服务，提供任务提交、状态查询、Agent Card 发现、健康检查与 Prometheus 指标等 REST API。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `serve_agent_card` | L53-L55 | 返回 A2A Agent Card 发现文档 |
| `validate` | L63-L95 | 接收进化验证请求，执行回归与新功能测试并返回判定 |
| `run_unit_tests` | L99-L122 | 编配单元测试，委派 SystemTest Agent 执行并返回结果 |
| `run_system_tests` | L126-L149 | 编配系统测试，委派 SystemTest Agent 执行并返回结果 |
| `get_validation_status` | L153-L158 | 查询指定验证任务的状态 |
| `health` | L166-L168 | 返回服务健康状态 |
| `metrics` | L172-L189 | 输出 Prometheus 格式的验证指标 |

---
## 文件：src/A2A/myorch/models.py

> 文件概述：定义MyOrch服务用于任务状态跟踪、测试编排和健康检查的Pydantic数据模型。

### 类列表

#### 类：`ValidationTask` (L24-L32)

> 类概述：保存验证任务的状态、请求和响应数据

#### 类：`HealthResponse` (L39-L42)

> 类概述：返回服务健康状态及名称

#### 类：`UnitTestOrchestrationRequest` (L49-L59)

> 类概述：封装单元测试编排的用例与路径参数

#### 类：`UnitTestOrchestrationResponse` (L62-L72)

> 类概述：返回单元测试执行的通过率与报告详情

#### 类：`SystemTestOrchestrationRequest` (L75-L85)

> 类概述：封装系统测试编排的用例与路径参数

#### 类：`SystemTestOrchestrationResponse` (L88-L98)

> 类概述：返回系统测试执行的通过率与报告详情

#### 类：`MetricsResponse` (L101-L108)

> 类概述：统计验证总次数、通过率及平均耗时

---
## 文件：src/A2A/myorch/orchestrator.py

> 文件概述：协调SystemTest Agent执行回归与新功能测试，根据通过率阈值判定验证结果并返回PASS/FAIL/ERROR。

### 类列表

#### 类：`MyOrchestrator` (L28-L440)

> 类概述：验证编排器，协调回归与新功能测试并判定结果。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L39-L61 |  |
| `run_validation` | L67-L145 |  |
| `get_status` | L147-L167 |  |
| `get_metrics` | L169-L171 | 返回全局验证指标统计数据。 |
| `_run_regression` | L177-L211 |  |
| `_run_new_feature_tests` | L213-L257 |  |
| `run_unit_test_orchestration` | L263-L344 |  |
| `run_system_test_orchestration` | L346-L410 |  |
| `_generate_summary` | L413-L436 |  |
| `close` | L438-L440 | 关闭 HTTP 客户端。 |

---
## 文件：src/A2A/shared/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/A2A/shared/config.py

> 文件概述：从环境变量加载并集中管理A2A各组件的连接、Docker、LLM评判等配置。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_env` | L11-L13 | 获取环境变量值，未设置时返回默认值。 |
| `load_config` | L16-L80 | 从环境变量加载并组织A2A所有配置项。 |

---
## 文件：src/A2A/shared/models.py

> 文件概述：定义 MyOrch 与 SystemTest 共享的验证任务、测试用例及报告数据结构。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `generate_task_id` | L162-L164 | 生成唯一的任务ID。 |
| `generate_test_id` | L167-L169 | 生成唯一的测试用例ID。 |

### 类列表

#### 类：`ValidationStatus` (L20-L26)

> 类概述：定义验证任务的状态枚举。

#### 类：`TestResult` (L29-L34)

> 类概述：定义单个测试用例的结果枚举。

#### 类：`EvolutionSpec` (L41-L48)

> 类概述：描述进化需求，包含标题、描述和回滚commit。

#### 类：`TestCase` (L55-L66)

> 类概述：定义新功能测试用例及其期望行为和评判类型。

#### 类：`ValidationRequest` (L73-L81)

> 类概述：MyClaude发起验证请求，携带进化规格与测试用例。

#### 类：`TestDetail` (L88-L95)

> 类概述：记录单个测试的执行结果、耗时与原始输出。

#### 类：`TestSuiteReport` (L102-L108)

> 类概述：汇总测试套件的通过率、详情和耗时。

#### 类：`ValidationReport` (L111-L117)

> 类概述：整合回归与新功能报告，给出整体通过判断。

#### 类：`ValidationResponse` (L124-L129)

> 类概述：返回验证结果，含任务ID、状态和报告。

#### 类：`ValidationStatusResponse` (L132-L136)

> 类概述：查询验证任务的状态与进度。

#### 类：`RegressionRequest` (L143-L147)

> 类概述：执行回归测试的请求，指定任务ID和可选的测试集。

#### 类：`NewFeatureTestRequest` (L150-L155)

> 类概述：执行新功能测试的请求，携带任务ID和测试用例。

---
## 文件：src/A2A/test/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/A2A/test/judge.py

> 文件概述：调用LLM比对测试用例实际输出与期望行为，判定PASS/FAIL/ERROR/INCONCLUSIVE。

### 类列表

#### 类：`LLMJudge` (L53-L210)

> 类概述：基于LLM比对实际输出与期望行为，判定测试是否通过

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L56-L59 | 初始化评判器，加载API密钥、基地址和模型名 |
| `evaluate` | L63-L112 | 评判一次测试，返回通过状态与简短理由 |
| `_call_llm` | L116-L171 | 调用LLM进行评判，最多重试3次防范空响应 |
| `_is_parsable_verdict` | L174-L185 | 检查响应字符串是否包含可解析的verdict JSON |
| `_parse_verdict` | L190-L210 | 从LLM返回内容中解析并返回测试状态 |

---
## 文件：src/A2A/test/models.py

> 文件概述：定义 SystemTest 服务的请求与响应数据模型，涵盖回归、新功能、单元及系统测试的交互结构。

### 类列表

#### 类：`TestRunState` (L28-L33)

> 类概述：枚举测试运行生命周期状态。

#### 类：`TestResult` (L40-L50)

> 类概述：表示新功能测试用例的详细执行结果。

#### 类：`RunRegressionRequest` (L53-L58)

> 类概述：封装回归测试的启动参数和选项。

#### 类：`RunRegressionResponse` (L61-L70)

> 类概述：返回回归测试的统计与详情。

#### 类：`RunNewFeatureRequest` (L73-L79)

> 类概述：携带新功能测试用例及变更文件列表。

#### 类：`RunNewFeatureResponse` (L82-L91)

> 类概述：返回新功能测试的统计与结果明细。

#### 类：`HealthResponse` (L94-L97)

> 类概述：返回服务健康状态标识。

#### 类：`UnitTestCase` (L100-L108)

> 类概述：定义直接调用函数的单元测试用例结构。

#### 类：`UnitTestResult` (L111-L118)

> 类概述：记录单个单元测试的执行状态与输出。

#### 类：`RunUnitTestRequest` (L121-L126)

> 类概述：提交单元测试用例集以启动执行。

#### 类：`RunUnitTestResponse` (L129-L138)

> 类概述：返回单元测试的统计与执行详情。

#### 类：`RunSystemTestRequest` (L141-L146)

> 类概述：提交系统测试用例字典列表。

#### 类：`RunSystemTestResponse` (L149-L158)

> 类概述：返回系统测试的统计与结果详情。

#### 类：`SandboxStatus` (L161-L165)

> 类概述：描述沙箱可用性及版本信息。

---
## 文件：src/A2A/test/sandbox.py

> 文件概述：提供 MyClaude 的 Docker 隔离运行环境，支持容器创建、命令执行与销毁，Docker 不可用时自动降级为子进程调用。

### 类列表

#### 类：`Sandbox` (L23-L222)

> 类概述：封装Docker容器或本地子进程的沙箱实例

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L26-L28 |  |
| `destroy` | L32-L44 |  |
| `run_myclaude_command` | L48-L55 | 在沙箱内执行 MyClaude 命令，返回标准输出、错误和退出码。 |
| `run_myclaude_command_with_test_output` | L59-L107 | 执行MyClaude测试命令并返回结构化JSON结果。 |
| `_run_in_docker_with_test_output` | L113-L134 |  |
| `_run_locally_with_test_output` | L139-L176 |  |
| `_run_in_docker` | L180-L198 |  |
| `_run_locally` | L203-L222 |  |

#### 类：`SandboxManager` (L225-L386)

> 类概述：管理多个Docker沙箱容器的生命周期，防泄漏

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L231-L233 |  |
| `is_available` | L237-L241 | 检测 Docker 可用性并缓存结果。 |
| `acquire` | L245-L251 |  |
| `release` | L255-L273 |  |
| `release_all` | L277-L279 |  |
| `active_count` | L284-L286 | 返回当前活跃沙箱 |
| `_check_docker` | L293-L301 |  |
| `_create_docker_sandbox` | L305-L386 |  |

---
## 文件：src/A2A/test/st/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/A2A/test/st/agent_card.py

> 文件概述：定义系统测试A2A代理的身份、技能与服务端点，构建返回统一AgentCard对象。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_get_service_url` | L31-L45 | 获取服务URL，优先环境变量，其次配置，默认本地。 |
| `get_agent_card` | L48-L73 | 构建并返回包含技能和URL的AgentCard。 |

---
## 文件：src/A2A/test/st/main.py

> 文件概述：提供系统测试执行的 A2A REST API，在沙箱中运行测试用例并通过 LLM 评判结果，支持生成 Excel 报告。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `serve_agent_card` | L41-L43 | 返回A2A Agent Card发现端点的JSON响应 |
| `health` | L49-L54 | 返回服务健康状态和Docker可用性 |
| `run_system_tests` | L60-L101 | 在沙箱中执行系统测试，LLM评判并生成Excel报告 |
| `metrics` | L107-L113 | 提供简化的Prometheus指标端点 |

---
## 文件：src/A2A/test/st/system_test_runner.py

> 文件概述：从 JSON 加载系统测试用例，在沙箱执行 MyClaude，由 LLM 评判结果并生成 Excel 报告。

### 类列表

#### 类：`SystemTestRunner` (L28-L493)

> 类概述：在沙箱执行MyClaude系统测试，LL

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L35-L37 |  |
| `execute` | L41-L75 |  |
| `load_test_cases` | L80-L100 |  |
| `_normalize_case` | L105-L127 |  |
| `_run_one` | L131-L207 | 在沙箱中运行用例并调用LLM评判，返回测试结果。 |
| `_strip_ansi` | L212-L233 | 去除文本中的 ANSI 转义和控制字符，返回清洁纯文本。 |
| `_build_actual_output` | L236-L334 |  |
| `generate_excel_report` | L339-L493 | 根据测试结果生成样式化的Excel报告并保存到指定目录 |

---
## 文件：src/A2A/test/ut/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/A2A/test/ut/agent_card.py

> 文件概述：定义单元测试服务的A2A代理卡片，描述身份、技能和端点，支持动态导入并LLM评判。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_get_service_url` | L32-L46 | 从环境变量或配置获取服务URL，默认返回本地地址 |
| `get_agent_card` | L49-L74 | 构建单元测试服务的A2A代理卡片，含技能和端点 |

---
## 文件：src/A2A/test/ut/main.py

> 文件概述：基于FastAPI的单元测试执行服务，提供REST和CLI接口，利用LLM评判结果并生成Excel报告。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `serve_agent_card` | L40-L42 | 返回 A2A Agent Card 发现文档 JSON。 |
| `health` | L48-L52 | 返回服务状态 ok 与名称 unittest。 |
| `run_unit_tests` | L58-L99 | 执行单元测试用例，通过 LLM 评判并返回报告。 |
| `metrics` | L105-L110 | 返回服务名与运行时间的简约指标。 |

---
## 文件：src/A2A/test/ut/unit_test_runner.py

> 文件概述：直接导入被测模块调用函数，捕获返回值与异常，交LLMJudge评判结果并生成Excel报告。

### 类列表

#### 类：`UnitTestRunner` (L23-L777)

> 类概述：执行单元测试并调用LLM评判，生成Excel报告。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L27-L28 | 初始化单元测试执行器并注入LLMJudge |
| `execute` | L32-L61 |  |
| `load_test_cases` | L66-L86 | 从 JSON 文件加载单元 |
| `_normalize_case` | L91-L114 |  |
| `_run_one` | L118-L167 |  |
| `generate_excel_report` | L172-L308 | 将测试结果导出为格式化的 Excel 报告文件。 |
| `_invoke_target` | L313-L404 |  |
| `_build_args` | L409-L530 | 解析 test_input 构造动态调用所需的 args 和 kwargs |
| `_parse_test_input_kv` | L535-L777 |  |

---
## 文件：src/chicken_farm/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/chicken_farm/cli.py

> 文件概述：解析并路由养鸡场管理系统的CLI命令，涵盖鸡群、产蛋、饲料、健康、销售、看板及数据导出等子功能。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_ensure_db` | L34-L36 | 初始化数据库 |
| `_print_table` | L41-L55 | 渲染Rich表格并打印到控制台 |
| `_print_panel` | L58-L63 | 渲染Rich面板并打印到控制台 |
| `handle_farm_command` | L119-L155 | 解析农场命令并分派到对应子命令 |
| `_handle_flock` | L160-L298 | 处理鸡群批次管理的增删改查等子命令 |
| `_handle_egg` | L303-L461 | 处理产蛋记录管理的增删改查与统计 |
| `_handle_feed` | L466-L608 | 处理饲料库存与消耗记录的增删改查及统计 |
| `_handle_health` | L613-L793 | 处理健康防疫记录的增删改查、提醒与统计 |
| `_handle_sales` | L798-L994 | 处理销售记录的增删改查与今日汇总统计 |
| `_handle_dashboard` | L999-L1084 | 渲染运营看板，支持产蛋趋势、料蛋比与收入报表 |
| `_handle_export` | L1089-L1118 | 导出数据库备份到指定目录 |

---
## 文件：src/chicken_farm/dashboard.py

> 文件概述：提供鸡场看板总览、批次概览、产蛋趋势、料蛋比、收入汇总及数据库导出功能。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `render_dashboard` | L11-L62 | 汇总存栏量、批次数、日产蛋等看板关键指标 |
| `flock_status_summary` | L65-L112 | 汇总活跃批次日龄、存栏及7天平均产蛋率 |
| `production_trend_report` | L115-L154 | 返回产蛋趋势，含每日产量、破蛋及破蛋率 |
| `feed_egg_ratio_report` | L157-L202 | 计算各活跃批次近7天饲料消耗与产蛋量比值 |
| `revenue_summary_report` | L205-L246 | 计算最近N个月的鸡蛋与淘汰鸡收入汇总 |
| `export_database` | L249-L273 | 将数据库备份文件导出到指定路径 |

---
## 文件：src/chicken_farm/database.py

> 文件概述：管理SQLite数据库连接，创建鸡群、产蛋、饲料、健康及销售记录表，支持初始化与增量迁移。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `set_db_path` | L12-L18 | 设置全局数据库文件路径并创建目录 |
| `get_db_path` | L21-L26 | 获取数据库路径，未设置时使用默认路径 |
| `get_connection` | L29-L35 | 返回启用了外键与WAL模式的SQLite连接 |
| `init_db` | L138-L146 | 执行建表SQL，创建所有缺失的数据表 |
| `migrate` | L149-L160 | 执行增量迁移，当前预留扩展接口 |
| `get_table_names` | L163-L172 | 查询并返回数据库中所有用户表名 |
| `table_exists` | L175-L177 | 判断指定表是否存在 |

---
## 文件：src/chicken_farm/egg_service.py

> 文件概述：提供产蛋记录的增删改查与统计分析，支持批次校验、日期查重、合格蛋自动计算及产蛋率汇总。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `add_egg_record` | L8-L60 | 录入产蛋记录，校验重复日期并自动计算合格蛋数。 |
| `list_egg_records` | L63-L91 | 查询产蛋记录，支持批次日期筛选，按日期倒序。 |
| `egg_stats` | L94-L195 | 统计总产量、日均、破损率及产蛋率，可按批次日期筛选。 |
| `edit_egg_record` | L198-L235 | 修改产蛋记录并重算合格蛋数，校验破损合法性。 |
| `delete_egg_record` | L238-L254 | 删除指定产蛋记录，若不存在则返回错误。 |

---
## 文件：src/chicken_farm/feed_service.py

> 文件概述：管理饲料采购入库、库存预警、消耗记录、料蛋比统计及删除恢复库存。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `add_feed_inventory` | L9-L45 | 验证参数并入库饲料采购记录，返回结果和记录ID。 |
| `list_feed_inventory` | L48-L84 | 查询饲料库存，添加低库存与过期预警标记后返回。 |
| `record_feed_consumption` | L87-L140 | 校验库存后记录饲料消耗并扣减数量，返回执行结果。 |
| `feed_stats` | L143-L251 | 统计饲料消耗总量，按批次、类型汇总并计算料蛋比。 |
| `delete_feed_consumption` | L254-L289 | 删除消耗记录，恢复库存数量并返回操作结果。 |

---
## 文件：src/chicken_farm/flock_service.py

> 文件概述：提供鸡群批次创建、列表查询、详情汇总、更新与删除等核心业务操作，含重名校验与关联数据检查。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `create_flock` | L10-L50 | 创建鸡群批次，校验名称唯一性和数量非负 |
| `list_flocks` | L53-L68 | 按状态筛选批次列表，结果按入栏日期倒序 |
| `update_flock` | L71-L144 | 更新批次数量、状态等字段，记录变更备注 |
| `get_flock_detail` | L147-L205 | 获取批次详情并汇总产蛋、饲料、健康、销售数据 |
| `delete_flock` | L208-L242 | 删除批次前校验是否存在关联业务记录 |

---
## 文件：src/chicken_farm/health_service.py

> 文件概述：实现鸡场健康防疫记录的录入、查询、疫苗提醒、费用统计及增删改管理。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `add_health_record` | L9-L47 | 录入健康记录并验证批次与费用，返回结果。 |
| `list_health_records` | L50-L82 | 按批次、类型、日期筛选查询健康记录，倒序返回。 |
| `vaccination_reminder` | L85-L156 | 检测活跃批次疫苗间隔，返回按紧急程度排序的提醒列表。 |
| `health_stats` | L159-L246 | 统计健康费用，按批次和类型汇总总费用与记录数。 |
| `edit_health_record` | L249-L298 | 更新健康记录的描述、费用、备注或类型。 |
| `delete_health_record` | L301-L320 | 删除指定健康记录，返回操作结果。 |

---
## 文件：src/chicken_farm/models.py

> 文件概述：定义鸡群、产蛋、饲料库存与消耗、健康防疫及销售等业务数据的 dataclass 模型。

### 类列表

#### 类：`Flock` (L8-L20)

> 类概述：鸡群批次数据模型，含品种、入栏日期、存栏数量等字段。

#### 类：`EggRecord` (L24-L33)

> 类概述：产蛋记录，包括总蛋数、破损数、合格数等。

#### 类：`FeedInventory` (L37-L47)

> 类概述：饲料库存信息，含类型、数量、采购日期等。

#### 类：`FeedConsumption` (L51-L58)

> 类概述：饲料消耗记录，关联鸡群和饲料。

#### 类：`HealthRecord` (L62-L71)

> 类概述：健康防疫记录，涵盖疫苗、疾病、用药。

#### 类：`SalesRecord` (L75-L86)

> 类概述：销售记录，支持鸡蛋或淘汰鸡销售。

---
## 文件：src/chicken_farm/sales_service.py

> 文件概述：实现销售记录录入、查询、统计、当日汇总、修改与删除，支持按批次和日期筛选。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `add_sales_record` | L9-L53 | 录入销售记录并自动计算总金额，返回创建结果。 |
| `list_sales_records` | L56-L88 | 按批次、类型或日期筛选销售记录，结果按日期倒序排列。 |
| `sales_stats` | L91-L189 | 按批次和产品类型汇总销售收入与数量。 |
| `today_sales_summary` | L192-L223 | 获取当日销售汇总，包括总记录数、总量和总金额。 |
| `edit_sales_record` | L226-L269 | 修改销售记录，自动更新总金额并返回操作结果。 |
| `delete_sales_record` | L272-L292 | 删除指定销售记录并返回操作结果。 |

---
## 文件：src/cli/cli_print.py

> 文件概述：实现命令行界面的富文本打印、打字机效果显示及对话内容的HTML记录与保存。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_init_win_vt` | L57-L76 | 启用 Windows 虚拟终端处理和自动换行 |
| `_append_html` | L102-L104 | 将 HTML 片段追加到全局输出缓冲区 |
| `save_buffer_to_file` | L107-L162 | 将累积 HTML 保存为文件，支持 .html/.doc 等格式 |
| `clear_screen` | L165-L167 | 清空终端屏幕 |
| `print_error` | L170-L174 | 格式化打印错误消息，带红色错误标志 |
| `print_info` | L177-L181 | 打印带空行的通用提示信息 |
| `print_detail` | L184-L188 | 打印紧跟前置信息的附属详细信息 |
| `print_user_input` | L191-L220 | 渲染用户输入消息带时间戳面板，记录交互起点 |
| `print_welcome` | L223-L272 | 输出欢迎界面及使用说明 |
| `print_banner` | L275-L291 | 打印彩色 ASCII 艺术 Banner |
| `print_header` | L294-L307 | 打印包含会话 ID 和时间的表头 |
| `print_timestamp` | L310-L314 | 打印助手 Bot 名称与时间戳 |
| `print_blank` | L318-L319 | 输出一个空行 |
| `typewriter_print` | L322-L343 | 打字机效果逐字输出带样式的文本 |
| `typewriter_then_markdown` | L346-L396 | 打字机效果后渲染 Markdown，长文本直接渲染 |
| `show_history` | L399-L421 | 以表格展示对话历史预览 |
| `show_token_count` | L424-L445 | 显示 API 返回的 Token 精确统计表 |
| `print_command_list` | L448-L473 | 打印可用斜杠命令列表，支持前缀筛选 |
| `print_command_invoked` | L476-L491 | 打印命令调用信息，含参数和来源 |
| `print_command_unknown` | L494-L504 | 提示未知命令，列出可用命令 |
| `print_unknown_cmd` | L507-L509 | 打印未知命令错误并提示帮助 |
| `typewriter_then_collapse` | L512-L578 | 打字机展示推理过程后折叠为摘要行 |
| `print_tool_call` | L581-L604 | 打印工具调用预告，显示关键参数 |
| `print_ask_user_question` | L607-L656 | 渲染 AI 提问面板并获取用户输入 |
| `print_tool_result` | L659-L717 | 打印工具结果，对长输出或特定命令折叠 |
| `set_reasoning_text` | L729-L743 | 存储某轮推理文本，供展开折叠使用 |
| `reset_reasoning` | L746-L752 | 清空所有推理记录和状态 |
| `expand_reasoning` | L755-L792 | 展开并显示指定轮次的完整推理内容 |
| `fold_reasoning` | L795-L813 | 将当前展开的推理折叠为预览 |
| `_show_reasoning_folded` | L816-L830 | 以折叠形式显示推理过程预览 |
| `show_status` | L834-L842 | 显示状态动画的上下文管理器 |
| `print_todo_list` | L845-L920 | 渲染 Todo 列表，展示进度和各条目状态 |
| `get_input` | L923-L973 | 获取用户输入，使用原生 input 避免重叠 |

---
## 文件：src/cli/code_statistics.py

> 文件概述：统计项目代码行数，按目录与扩展名汇总总行、代码、空行及注释行，并通过CLI命令表格化输出。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `parse_gitignore_skip_dirs` | L27-L68 | 解析.gitignore，提取跳过的目录名和路径前缀。 |
| `count_comments` | L73-L88 | 根据扩展名统计代码行中的注释数量。 |
| `file_stats` | L93-L108 | 统计文件总行数、代码行、空行和注释行。 |
| `collect_stats` | L113-L169 | 遍历根目录，返回按目录和扩展名汇总的统计。 |
| `print_dir_table` | L174-L218 | 以表格形式输出指定目录的文件统计。 |
| `print_summary_table` | L221-L260 | 打印按扩展名汇总的项目总计表。 |
| `code_statistics` | L265-L288 | 统计项目代码行数，支持按目录和扩展名输出。 |
| `main` | L292-L294 | 独立命令行入口，调用代码统计主函数。 |

---
## 文件：src/cli/h2m.py

> 文件概述：将MyClaude会话日志HTML转为Markdown，支持按轮次和小节筛选内容。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_resolve_path` | L135-L146 | 解析文件路径，纯文件名时拼接到日志根目录 |
| `_parse_turns` | L149-L159 | 解析逗号分隔的轮次字符串为整数列表 |
| `_parse_sections` | L162-L166 | 解析逗号分隔的小节标题字符串为列表 |
| `_strip_emoji_prefix` | L169-L171 | 去除文本开头的Emoji和空白字符 |
| `_format_memory_content` | L174-L210 | 处理记忆内容，添加父节点并删除重复相关性 |
| `_build_markdown` | L213-L289 | 按轮次和小节筛选结构化数据生成Markdown文本 |
| `convert_html_to_markdown` | L292-L348 | 将Session Log HTML转换为Markdown文件并返回结果 |
| `main` | L351-L382 | 命令行入口，解析参数并执行HTML到Markdown转换 |

### 类列表

#### 类：`_SessionHTMLParser` (L19-L132)

> 类概述：解析HTML提取会话标题、时间和带小节的轮次数据

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L44-L59 |  |
| `handle_starttag` | L61-L82 |  |
| `handle_endtag` | L85-L103 |  |
| `handle_data` | L105-L132 |  |

---
## 文件：src/cli/mycli.py

> 文件概述：实现 MyClaude Code 风格的命令行交互界面，支持记忆提取、整理、进化等操作的报告生成。 (截断)

### 类列表

#### 类：`MyClaudeCLI` (L9-L2917)

> 类概述：MyClaude 风格命令行交互界面类

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L13-L25 | 初始化查询循环与斜杠命令系统 |
| `_format_duration` | L31-L65 | 毫秒耗时转为可读时间字符串 |
| `_format_estimated_time` | L68-L94 | 按条目数预估 LLM 处理耗时 |
| `_save_extraction_report` | L97-L174 | 保存记忆提取明细为Markdown报告 |
| `_save_compaction_report` | L177-L276 | 保存记忆整理报告为Markdown |
| `_save_evolution_report` | L279-L447 | 保存记忆进化报告为Markdown |
| `_save_bug_extraction_report` | L450-L516 | 保存Bug提取明细为Markdown报告 |
| `handle_command` | L518-L1584 | 处理用户命令，返回是否继续会话 |
| `_get_bug_base` | L1587-L1603 | 获取或初始化BugBase实例 |
| `_handle_bug_show` | L1605-L1655 | 显示Bug库统计信息 |
| `_handle_bug_extract` | L1657-L1801 | 从MD日志提取Bug记录 |
| `_handle_bug_retrieve` | L1803-L1822 | 按模块或文件名召回Bug |
| `_handle_bug_remove` | L1824-L1861 | 清除所有Bug记录 |
| `_print_bug_list` | L1864-L1873 | 打印Bug记录列表 |
| `_should_log_cli_command` | L1879-L1890 | 判断CLI命令是否需记录到上下文 |
| `run` | L1892-L1927 | 运行CLI主循环，支持聊天与工具 |
| `run_test_mode` | L1930-L2084 | 测试模式：执行一次查询并输出JSON结果 |
| `_run_unit_test_exec` | L2087-L2258 | 执行单元测试并输出进度与报告 |
| `_check_port_open` | L2261-L2268 | 检查端口是否在监听 |
| `_ensure_a2a_services` | L2271-L2381 | 检查并启动所需A2A服务 |
| `_run_unit_test_a2a` | L2384-L2545 | 通过A2A协议执行单元测试 |
| `_run_system_test_exec` | L2548-L2720 | 执行系统测试并输出进度与报告 |
| `_run_system_test_a2a` | L2723-L2917 | 通过A2A协议执行系统测试 |

---
## 文件：src/cli/test_progress.py

> 文件概述：提供带spinner动画和实时成功率的测试进度显示，统一测试命令的进度展示形式。

### 类列表

#### 类：`TestProgressDisplay` (L13-L118)

> 类概述：带旋转动画与实时通过率的测试进度显示器

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L29-L49 | 初始化总数、测试类型与内部状态锁 |
| `start` | L51-L55 | 启动后台线程播放旋转动画 |
| `update` | L57-L61 | 线程安全地更新完成与通过计数 |
| `_spin` | L63-L95 | 循环刷新进度、通过率和耗时动画 |
| `stop` | L97-L104 | 停止动画并清除终端当前行 |
| `print_final_progress` | L106-L118 | 输出最终执行与通过数量的统计 |

---
## 文件：src/cli/tree_visualizer.py

> 文件概述：递归扫描项目目录，借助大模型生成文件功能概述，输出带注释的树形目录结构图。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `parse_gitignore` | L35-L74 | 解析项目根目录的.gitignore文件，返回规则列表 |
| `is_ignored_by_gitignore` | L77-L116 | 按规则顺序判断相对路径是否被忽略 |
| `_match_pattern` | L119-L124 | 使用fnmatch进行glob模式匹配，支持** |
| `_match_globstar` | L127-L145 | 用正则实现支持**的glob匹配 |
| `is_binary_extension` | L162-L164 | 检查文件扩展名是否属于二进制排除列表 |
| `load_cache` | L206-L248 | 从缓存表格文件加载文件路径到时间戳与概述的映射 |
| `save_cache` | L251-L264 | 将缓存保存为表格格式的独立文件 |
| `get_file_overview` | L286-L373 | 获取文件概述，优先缓存，否则调用LLM并打印 |
| `build_tree` | L380-L473 | 递归构建rich Tree，过滤忽略项并附上概述 |
| `_format_label` | L476-L481 | 格式化树节点标签，使概述列对齐 |
| `_save_tree_to_file` | L488-L512 | 将rich Tree渲染为纯文本保存到缓存文件 |
| `extract_ast_info` | L519-L575 | 静态解析Python文件，提取函数和类的名称与行范围 |
| `batch_generate_summaries` | L596-L673 | 批量调用LLM为文件中所有函数/类生成概述 |
| `_fill_summaries` | L676-L686 | 将LLM返回的概述回填到FileInfo对应对象中 |
| `_fill_failed_summaries` | L689-L699 | 为所有未生成概述的函数/类填充失败文本 |
| `_fallback_individual_summaries` | L717-L744 | 降级为逐个请求LLM为未匹配项生成概述 |
| `_request_single_summary` | L747-L770 | 请求LLM为单个函数或类生成概述 |
| `format_file_detail` | L773-L818 | 将FileInfo格式化为包含函数和类概述的Markdown片段 |
| `load_init_record` | L825-L849 | 加载增量记录文件，返回路径到时间戳的映射 |
| `save_init_record` | L852-L864 | 将增量记录保存为人类可读的日期格式JSON |
| `should_generate` | L867-L900 | 基于摘要文件存在性和mtime判断是否需要重新生成 |
| `parse_old_summary_file` | L903-L927 | 解析旧摘要文件，提取各文件对应的Markdown片段 |
| `_is_under_ignored_dir` | L938-L949 | 检查文件是否位于被gitignore忽略的目录内 |
| `_format_duration_s` | L956-L963 | 将秒数转换为时分秒格式的可读时间字符串 |
| `_format_estimated_time` | L966-L973 | 格式化预估时间，输出约X分Y秒等描述 |
| `_prescan_project_files` | L976-L988 | 预扫描项目，统计需要处理的总文件、新增和缓存数 |
| `_prescan_recursive` | L991-L1065 | 递归遍历目录，收集文件并统计缓存和新文件数量 |
| `_prescan_python_files` | L1068-L1125 | 预扫描Python文件，统计需生成摘要的文件和函数数量 |
| `create_project_tree` | L1128-L1363 | 创建项目工程树，支持文件级或函数级摘要生成 |
| `main` | L1367-L1392 | 命令行入口，解析参数并调用create_project_tree |

### 类列表

#### 类：`FuncInfo` (L172-L178)

> 类概述：存储函数/方法的名字、行范围和文档字符串与概述

#### 类：`ClassInfo` (L182-L189)

> 类概述：存储类名、行范围、文档字符串、概述及内部方法列表

#### 类：`FileInfo` (L193-L199)

> 类概述：存储文件路径、概述、函数列表、类列表及完整源码

---
## 文件：src/command/__init__.py

> 文件概述：

### 类列表

#### 类：`CommandInfo` (L7-L15)

> 类概述：存储从 Markdown 文件扫描的斜杠命令信息

---
## 文件：src/command/dispatcher.py

> 文件概述：解析用户输入的斜杠命令，查找已注册命令并提取参数，组装上下文注入LLM。

### 类列表

#### 类：`CommandDispatcher` (L7-L60)

> 类概述：判断斜杠命令并将命令注入LLM上下文

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L10-L11 | 初始化分发器，存储命令注册表实例。 |
| `parse_and_lookup` | L13-L34 | 解析斜杠命令输入，匹配最长命令 |
| `extract_argument` | L36-L46 |  |
| `build_context` | L48-L60 |  |

---
## 文件：src/command/registry.py

> 文件概述：维护斜杠命令映射表，支持注册、查询、列表及判断用户输入是否为命令。

### 类列表

#### 类：`CommandRegistry` (L6-L33)

> 类概述：维护所有已注册命令的映射表，提供查询接口。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L9-L10 | 初始化命令字典为空。 |
| `register` | L12-L14 | 将命令信息存入映射表。 |
| `get` | L16-L18 |  |
| `list_commands` | L20-L22 |  |
| `list_command_names` | L24-L26 |  |
| `is_command` | L28-L33 |  |

---
## 文件：src/command/scanner.py

> 文件概述：递归扫描 .myclaude/ 目录下的 Markdown 文件，提取元数据并注册为斜杠命令。

### 类列表

#### 类：`CommandScanner` (L11-L82)

> 类概述：扫描命令目录，将.md文件注册为斜杠命令。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L14-L16 | 初始化项目根目录和命令目录路径。 |
| `scan` | L18-L33 | 遍历命令目录，注册所有Markdown文件到注册表。 |
| `_path_to_command` | L35-L45 | 将文件路径转换为斜杠命令名。 |
| `_parse_file` | L47-L82 | 解析Markdown文件，提取元数据和正文。 |

---
## 文件：src/llm_tool/cmd_bash.py

> 文件概述：执行安全的shell命令，拦截占位符和文件内容搜索外部工具，强制UTF-8输出。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `tool_bash` | L13-L47 | 拒绝无效命令和文件搜索，以UTF-8执行shell并返回输出。 |

---
## 文件：src/llm_tool/re_test.py

> 文件概述：解析并提取响应文本中 `<str_replace>` 标签的路径、摘要及新旧内容。

(无函数或类定义)

---
## 文件：src/llm_tool/tool_executor.py

> 文件概述：解析AI响应中的XML工具调用，支持嵌套感知与标签泄露清理，并将工具块转为结构化参数。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_final_clean_xml_tags` | L19-L37 | 移除独占一行的XML工具标签残留，避免破坏内嵌合法标签。 |
| `_find_container_end` | L40-L143 | 嵌套感知查找容器闭合标签，忽略字符串、Markdown代码块内的匹配。 |
| `_extract_subtag_content` | L146-L173 | 从块中提取子标签内容，支持嵌套并返回补偿内容。 |
| `_parse_str_replace_block` | L176-L271 | 解析str_replace块，提取old/new子标签并清理标签泄露。 |
| `parse_tools` | L274-L301 | 顺序解析AI响应中的XML工具调用，严格失败时宽松兜底。 |
| `_find_str_replace_end` | L304-L362 | 通过old/new子标签定位str_replace闭合位置，避免内容干扰。 |
| `_find_markdown_code_ranges` | L365-L420 | 识别Markdown代码块与行内代码范围，排除工具标签解析。 |
| `_is_position_in_code` | L423-L428 | 判断某位置是否处于Markdown代码范围内。 |
| `_parse_tools_strict` | L431-L529 | 严格模式解析XML工具调用，处理容器/自闭合标签与嵌套。 |
| `_build_result` | L532-L652 | 将匹配结果构建为剩余文本和工具调用列表。 |
| `_parse_tools_loose` | L655-L759 | 宽松匹配畸形工具标签，容忍单引号和无闭合等格式。 |
| `execute_code_tool` | L766-L938 | 执行单个工具并返回API消息格式的结果。 |
| `execute_tools` | L941-L951 | 批量执行工具列表，返回API消息格式列表。 |

---
## 文件：src/memory/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/memory/factory.py

> 文件概述：根据配置动态加载记忆后端实例，支持空实现并统一返回MemoryInterface接口。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `create_memory` | L26-L81 | 根据配置动态加载并实例化记忆后端，支持空后端回退。 |
| `_get_backend_name` | L84-L92 | 从配置中提取记忆后端名称，兼容多种配置结构。 |
| `_check_module_importable` | L95-L103 | 验证后端模块路径可导入，失败则给出明确错误提示。 |

---
## 文件：src/memory/memory_2/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/memory/memory_2/adapter.py

> 文件概述：作为兼容层，使旧导入路径 Memory2Adapter 指向已自包含全部逻辑的 Memory2Backend。

### 类列表

#### 类：`Memory2Adapter` (L21-L34)

> 类概述：兼容适配器，继承 Memory2Backend，保持旧导入路径可用。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L32-L34 | 调用父类构造并记录调试日志。 |

---
## 文件：src/memory/memory_2/memory2.py

> 文件概述：重构的记忆后端，实现LLM检索、工作记忆管理、自动压缩与遗忘衰减，为LLM代理提供无向量的长期记忆保障。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `create_memory` | L478-L481 | 工厂函数，创建 Memory2Backend 实例 |

### 类列表

#### 类：`Memory2Backend` (L30-L475)

> 类概述：基于 LLM 的记忆后端，无向量依赖

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L38-L86 | 初始化配置、存储、检索器、压缩器与工作记忆 |
| `add` | L92-L115 | 添加记忆，去重并刷新工作记忆 |
| `get` | L117-L118 | 按 ID 获取单条记忆 |
| `search` | L120-L149 | LLM 召回检索并过滤低相关性结果 |
| `get_working_memory` | L151-L155 | 返回工作记忆的格式化上下文 |
| `get_context_for_query` | L157-L170 | 结合工作记忆评分与长期检索生成上下文 |
| `update` | L172-L173 | 更新指定记忆的字段 |
| `delete` | L175-L176 | 删除指定记忆 |
| `clear_all` | L178-L182 | 清空所有记忆和工作记忆 |
| `compact` | L184-L242 | 压缩短期记忆为摘要或清理超量条目 |
| `stats` | L244-L255 | 返回区分压缩与未压缩的记忆统计 |
| `maintain` | L257-L262 | 遗忘过期记忆并衰减重要性 |
| `set_llm_chat_fn` | L268-L272 | 注入 LLM 对话函数并同步到组件 |
| `_build_context_text` | L274-L283 | 构建最近工作记忆的文本上下文 |
| `_score_working_memory` | L285-L311 | 用 LLM 对工作记忆评分 |
| `_forget_expired` | L313-L330 | 删除超过 TTL 的未压缩短期记忆 |
| `_init_store` | L336-L356 | 初始化持久化存储实例 |
| `_init_retriever` | L358-L388 | 初始化检索器并加载提示词 |
| `_init_compressor` | L390-L397 | 初始化记忆压缩器 |
| `_build_llm_fn` | L400-L453 | 由配置构建 OpenAI 聊天函数 |
| `_default_config` | L456-L475 | 返回默认配置命名空间 |

---
## 文件：src/memory/memory_2/memory_compressor.py

> 文件概述：将多条短期记忆分批调用LLM总结，合并为一条精炼长期摘要，并标记原始记忆已压缩。

### 类列表

#### 类：`MemoryCompressor` (L24-L289)

> 类概述：将多条短期记忆压缩为一条长期记忆摘要。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L33-L50 | 初始化压缩器，配置模型、批大小和LLM回调。 |
| `set_llm_chat_fn` | L56-L58 | 更新LLM对话函数。 |
| `enabled` | L61-L63 | 返回压缩器是否启用。 |
| `should_compress` | L65-L80 | 判断记忆数量是否达到压缩阈值。 |
| `compress` | L82-L130 | 调用LLM将多条记忆压缩为摘要文本。 |
| `mark_compressed` | L132-L147 | 标记原始记忆为已压缩状态。 |
| `_split_into_batches` | L153-L178 | 按token估算将记忆列表分批。 |
| `_compress_batch` | L184-L211 | 调用LLM压缩单批记忆为摘要。 |
| `_merge_summaries` | L217-L248 | 将多个批次摘要合并为一条。 |
| `_format_batch_for_compression` | L254-L275 | 格式化一批记忆为压缩候选文本。 |
| `_compression_system_prompt` | L277-L289 | 返回压缩用系统提示词。 |

---
## 文件：src/memory/memory_2/memory_injector.py

> 文件概述：格式化工作记忆与检索结果，合并去重并按token预算截断，生成LLM上下文注入文本。

### 类列表

#### 类：`MemoryInjector` (L24-L187)

> 类概述：纯格式化、无状态的记忆上下文注入器

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L27-L39 | 初始化 token 预算与字符估算系数 |
| `get_max_chars` | L45-L46 | 返回允许的最大字符数 |
| `to_log_entry` | L49-L50 | 将上下文文本转为日志条目字典 |
| `format_working_memory` | L56-L74 | 将工作记忆条目列表格式化为注入文本 |
| `format_context` | L76-L111 | 合并去重工作记忆与检索结果并格式化 |
| `format_search_results` | L113-L123 | 向后兼容的搜索结果注入格式化入口 |
| `_merge_and_deduplicate` | L129-L166 | 按 content_hash 合并去重记忆条目列表 |
| `_truncate_by_tokens` | L172-L187 | 按 token 预算截断文本并添加截断标记 |

---
## 文件：src/memory/memory_2/memory_retriever.py

> 文件概述：不依赖向量索引，将记忆直接交给LLM评分相关性，完成候选预过滤与结构化精排，并回写分数、排序返回。

### 类列表

#### 类：`MemoryRetriever` (L85-L763)

> 类概述：LLM召回检索器：负责候选预过滤、LLM评分、解析与综合打分。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L96-L136 |  |
| `set_llm_chat_fn` | L138-L140 |  |
| `search` | L147-L252 |  |
| `_get_candidates` | L258-L294 |  |
| `_split_into_batches` | L300-L309 |  |
| `_cross_batch_normalize` | L315-L390 |  |
| `_get_cached_scores` | L396-L404 |  |
| `_set_cached_scores` | L406-L413 |  |
| `_score_batch` | L419-L468 |  |
| `_parse_scores` | L474-L537 |  |
| `_try_parse_json_array` | L540-L590 |  |
| `_fallback_parse_scores` | L592-L637 |  |
| `_compute_final_scores` | L643-L672 |  |
| `score_external_items` | L674-L733 |  |
| `_compute_cache_key` | L735-L738 |  |
| `_writeback_scores` | L744-L751 |  |
| `default_top_k` | L758-L759 |  |
| `max_top_k` | L762-L763 |  |

---
## 文件：src/memory/memory_2/memory_store.py

> 文件概述：持久化记忆条目为JSON，支持去重、备份恢复、标签时间角色过滤与重要性衰减。

### 类列表

#### 类：`MemoryStore` (L26-L372)

> 类概述：纯文本JSON记忆持久化存储，支持内容去重与备份恢复。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L40-L46 | 初始化文件路径、备份数量，加载已有数据。 |
| `add` | L52-L81 | 添加记忆条目，重复内容返回已有ID。 |
| `get` | L83-L87 | 按ID获取记忆条目副本。 |
| `update` | L89-L96 | 更新指定记忆字段并保存。 |
| `delete` | L98-L105 | 删除指定记忆并重建哈希索引。 |
| `get_all` | L107-L108 | 返回所有记忆条目的副本列表。 |
| `get_by_ids` | L110-L112 | 根据ID列表批量获取记忆。 |
| `delete_by_ids` | L114-L122 | 批量删除记忆并重建哈希索引。 |
| `count` | L128-L129 | 返回记忆总数。 |
| `count_compressed` | L131-L132 | 返回已压缩记忆数量。 |
| `count_uncompressed` | L134-L135 | 返回未压缩记忆数量。 |
| `get_stats` | L137-L153 | 返回统计信息（总数、压缩数、角色标签分布）。 |
| `query` | L159-L194 | 按标签、时间窗口、角色过滤，可选包含压缩摘要。 |
| `batch_update_scores` | L196-L205 | 批量更新记忆的最后评分。 |
| `decay_importance` | L207-L218 | 对已压缩记忆执行重要性衰减。 |
| `clear_all` | L220-L234 | 清空所有记忆及持久化文件。 |
| `load` | L240-L268 | 从JSON文件加载数据，兼容旧格式。 |
| `_save` | L270-L281 | 安全存储数据并轮转备份文件。 |
| `_rotate_backups` | L287-L303 | 轮转备份文件，仅保留指定数量。 |
| `_glob_backups` | L305-L306 | 获取所有备份文件路径列表。 |
| `_restore_from_backup` | L308-L332 | 从最新备份恢复数据。 |
| `_ensure_directory` | L334-L335 | 确保存储目录存在。 |
| `_rebuild_hash_index` | L337-L341 | 根据当前数据重建内容哈希集合。 |
| `_compute_hash` | L344-L345 | 计算字符串的MD5哈希值。 |
| `_parse_timestamp` | L348-L364 | 解析时间戳为UTC datetime，失败返回最小值。 |
| `get_storage_path` | L366-L367 | 返回持久化文件路径。 |
| `get_file_size` | L369-L372 | 返回存储文件字节大小。 |

---
## 文件：src/memory/memory_interface.py

> 文件概述：定义记忆子系统的统一抽象接口及空操作实现，供业务层依赖注入编程。

### 类列表

#### 类：`MemoryInterface` (L14-L147)

> 类概述：定义记忆子系统抽象基类，声明必须实现的接口

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `add` | L25-L36 |  |
| `get` | L39-L48 |  |
| `search` | L51-L62 | 语义检索记忆，返回最相关的 top_k 条结果 |
| `get_working_memory` | L65-L71 |  |
| `get_context_for_query` | L74-L86 |  |
| `update` | L89-L99 |  |
| `delete` | L102-L111 | 删除指定ID的记忆，返回布尔值表示成功与否。 |
| `clear_all` | L114-L120 |  |
| `compact` | L123-L129 |  |
| `stats` | L132-L138 |  |
| `maintain` | L141-L147 | 执行记忆维护，清理过期项并优化索引 |

#### 类：`NoopMemory` (L150-L193)

> 类概述：空操作实现，所有方法返回默认值

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `add` | L156-L157 | 返回固定的无操作占位ID "noop-0"。 |
| `get` | L159-L160 |  |
| `search` | L162-L163 | 语义检索返回最相关的 top_k 条记忆。 |
| `get_working_memory` | L165-L166 | 获取本次会话的工作记忆上下文，返回格式化文本。 |
| `get_context_for_query` | L168-L169 |  |
| `update` | L171-L172 |  |
| `delete` | L174-L175 | 空操作实现，始终返回删除失败。 |
| `clear_all` | L177-L178 |  |
| `compact` | L180-L181 |  |
| `stats` | L183-L190 |  |
| `maintain` | L192-L193 |  |

---
## 文件：src/memory_ex/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/memory_ex/bug_base/__init__.py

> 文件概述：

(无函数或类定义)

---
## 文件：src/memory_ex/bug_base/bug_base.py

> 文件概述：Bug库系统主入口，协调Bug存储、提取、检索与注入组件，提供统一调用接口。

### 类列表

#### 类：`BugBase` (L15-L148)

> 类概述：协调Bug库各组件，提供统一接口

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L18-L29 |  |
| `extract_from_raw_entries` | L31-L39 |  |
| `extract_from_md_logs` | L41-L51 |  |
| `extract_from_session` | L53-L61 | 从对话中提取Bug并存入Bug库，返回新增Bug ID列表。 |
| `retrieve_and_inject` | L63-L74 |  |
| `retrieve` | L76-L84 |  |
| `mark_memory_linked` | L86-L92 | 标记Bug已关联到Memory记录。 |
| `get_stats` | L94-L96 |  |
| `get_record` | L98-L100 | 按 ID 获取 Bug 记录并返回。 |
| `get_all` | L102-L104 |  |
| `get_by_module` | L106-L108 | 获取指定模块的所有Bug记录。 |
| `get_by_file` | L110-L112 | 获取涉及指定文件的所有记录。 |
| `set_extract_progress_callback` | L114-L120 |  |
| `get_extraction_stats` | L122-L148 |  |

---
## 文件：src/memory_ex/bug_base/bug_extractor.py

> 文件概述：利用LLM从对话、原始记忆和MD日志中提取Bug，结构化存储并支持增量提取。

### 类列表

#### 类：`BugExtractor` (L15-L539)

> 类概述：从对话历史中提取Bug并结构化存储

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L20-L30 |  |
| `set_progress_callback` | L32-L38 |  |
| `extract_from_session` | L40-L112 |  |
| `extract_from_raw_entries` | L114-L299 |  |
| `_load_prompt` | L301-L306 |  |
| `extract_from_md_logs` | L308-L467 | 从MD会话日志增量提取Bug并返回统计信息。 |
| `_format_dialog` | L469-L479 |  |
| `_call_llm` | L481-L498 |  |
| `_parse_llm_response` | L500-L535 | 解析LLM返回的JSON，提取Bug记录列表。 |
| `_resolve_module` | L537-L539 |  |

---
## 文件：src/memory_ex/bug_base/bug_injector.py

> 文件概述：将历史Bug记录格式化为警示性LLM上下文，并控制注入文本的token预算。

### 类列表

#### 类：`BugInjector` (L10-L75)

> 类概述：将历史Bug记录格式化为警示注入上下文

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L13-L19 | 初始化并设置最大token预算 |
| `inject` | L21-L50 | 将记录列表转为注入文本并控制token预算 |
| `_format_record` | L52-L66 | 格式化单条Bug记录为警示文本 |
| `_estimate_tokens` | L68-L75 | 按中英文字符估算文本token数 |

---
## 文件：src/memory_ex/bug_base/bug_retriever.py

> 文件概述：基于路径粗筛与LLM语义精筛的两级召回Bug检索器。

### 类列表

#### 类：`BugRetriever` (L14-L199)

> 类概述：两级召回检索器，组合路径匹配与LLM语义过滤。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L19-L28 |  |
| `retrieve` | L30-L62 | 两级召回：路径匹配粗筛，LLM语义精筛，返回相关历史Bug记录。 |
| `_stage1_path_match` | L64-L81 | 根据路径精确匹配与模块扩展，召回所有相关开放Bug。 |
| `_stage2_semantic_filter` | L83-L133 |  |
| `_load_prompt` | L135-L146 |  |
| `_call_llm` | L148-L164 |  |
| `_parse_relevant_ids` | L166-L199 |  |

---
## 文件：src/memory_ex/bug_base/bug_store.py

> 文件概述：管理Bug库的纯Markdown文件存储，支持按模块分文件读写、查询、更新与归档，并提供增量提取和哈希校验。

### 类列表

#### 类：`BugRecord` (L16-L55)

> 类概述：Bug记录数据模型，包含根因、症状、修复方式等字段。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `to_dict` | L33-L35 |  |
| `from_dict` | L38-L55 | 将字典转换为BugRecord实例。 |

#### 类：`BugStore` (L58-L458)

> 类概述：Bug库存储层，管理纯Markdown文件存储与归档。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L74-L81 |  |
| `add` | L85-L95 |  |
| `get` | L97-L102 |  |
| `get_by_module` | L104-L106 |  |
| `get_by_file` | L108-L116 | 返回涉及指定文件的所有Bug记录。 |
| `update` | L118-L133 |  |
| `get_all` | L135-L137 |  |
| `get_stats` | L139-L149 | 统计各模块Bug记录数量。 |
| `_resolve_module` | L153-L167 |  |
| `_get_module_files` | L171-L173 |  |
| `_read_md` | L175-L193 |  |
| `_parse_md_record` | L195-L270 | 从Markdown片段 |
| `_write_md` | L272-L278 |  |
| `_format_md_record` | L280-L305 |  |
| `_iter_all_records` | L309-L314 |  |
| `_compute_file_hash` | L318-L323 |  |
| `_path_match` | L326-L337 |  |
| `generate_id` | L339-L352 |  |
| `get_extracted_entry_ids` | L354-L367 |  |
| `mark_entry_extracted` | L369-L381 |  |
| `get_extracted_md_files` | L383-L396 |  |
| `get_md_file_offset` | L398-L401 |  |
| `mark_md_file_extracted` | L403-L420 |  |
| `read_incremental_content` | L422-L458 |  |

---
## 文件：src/memory_ex/context_compressor.py

> 文件概述：监控Token水位，执行三段式上下文压缩（固定/摘要/滚动区）、工具结果截断及恢复期管理。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_load_prompt` | L28-L34 | 加载 Prompt 模板文件 |
| `_estimate_tokens` | L37-L39 | 按字符数/2.5粗略估算token数 |
| `_estimate_messages_tokens` | L42-L55 | 估算消息列表的总token数 |
| `_dict_to_namespace` | L624-L632 | 递归将字典转换为SimpleNamespace |

### 类列表

#### 类：`ContextCompressor` (L58-L621)

> 类概述：

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L77-L89 |  |
| `_load_compress_config` | L91-L127 |  |
| `set_llm_chat_fn` | L129-L131 |  |
| `compress` | L133-L207 | 会话上下文压缩：检测token水位，触发摘要生成或工具结果截断。 |
| `_full_compress` | L209-L319 | 执行完整压缩，含LLM摘要生成、合并与工具结果截断。 |
| `_light_compress` | L321-L356 |  |
| `_generate_summary` | L358-L419 |  |
| `_merge_summaries` | L421-L444 |  |
| `_extract_section` | L446-L450 |  |
| `_truncate_tool_results` | L452-L515 |  |
| `_truncate_content` | L517-L525 |  |
| `_truncate_file_view` | L527-L536 |  |
| `_is_likely_ending` | L538-L589 |  |
| `_has_tech_keywords` | L591-L597 |  |
| `_get_builtin_summary_prompt` | L599-L621 |  |

---
## 文件：src/memory_ex/factory.py

> 文件概述：根据配置动态创建记忆后端实例，支持MemoryEx与NoopMemory，并在失败时自动降级。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_get_backend_name` | L15-L40 | 从配置对象中读取 memory.backend 值并返回 |
| `create_memory` | L43-L81 | 根据配置创建记忆后端实例，失败时降级为 NoopMemory |

---
## 文件：src/memory_ex/memory_compactor.py

> 文件概述：通过规则与LLM辅助合并、淘汰低分记忆条目，控制记忆层水位，实现轻量自动整理。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_load_prompt` | L25-L31 | 加载指定文件名的 Prompt 模板内容。 |

### 类列表

#### 类：`MemoryCompactor` (L34-L630)

> 类概述：执行三段式整理流水线，控制 Layer 1 的大小与质量。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L41-L69 | 初始化记忆压缩器，加载配置与水位阈值。 |
| `set_llm_chat_fn` | L71-L73 |  |
| `set_progress_callback` | L75-L82 |  |
| `_report_progress` | L84-L90 |  |
| `check_needed` | L94-L109 |  |
| `run_auto_compaction` | L111-L133 | 按水位和配置自动触发完整或轻量记忆整理。 |
| `run_full_compaction` | L135-L197 |  |
| `evict_by_id` | L199-L227 | 根据 |
| `_step_merge` | L231-L258 |  |
| `_parse_layer1_entries` | L260-L294 |  |
| `_rule_based_merge` | L296-L380 |  |
| `_llm_assisted_merge` | L382-L445 | 利用LLM合并语义相似但标签不同的条目，压缩因果链。 |
| `_parse_merge_response` | L447-L465 |  |
| `_step_evict` | L469-L527 |  |
| `_rebuild_layer1` | L531-L538 |  |
| `_format_entry_line` | L540-L549 |  |
| `_same_tags` | L551-L559 |  |
| `_jaccard_similarity` | L561-L579 | 计算文本Jaccard相似度，基于字符级bigram集合。 |
| `_call_llm` | L581-L613 |  |
| `_get_builtin_merge_prompt` | L615-L630 | 返回内置合并Prompt模板字符串。 |

---
## 文件：src/memory_ex/memory_evolver.py

> 文件概述：通过对层1记忆的深度推理，发现模式、解决矛盾、归纳解法与洞察趋势，并以加法方式追加新认知。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_load_prompt` | L31-L37 | 加载 Prompt 模板文件。 |

### 类列表

#### 类：`MemoryEvolver` (L40-L770)

> 类概述：记忆进化器，通过推理发现隐含模式并生成高层认知。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L49-L74 |  |
| `set_llm_chat_fn` | L76-L78 | 注入LLM调用函数。 |
| `set_progress_callback` | L80-L86 |  |
| `get_unevolved_count` | L90-L96 |  |
| `check_needed` | L98-L104 |  |
| `_get_unevolved_from_layer1` | L106-L153 |  |
| `run_auto_evolution` | L155-L167 | 检查配置开关和累积量，满足条件则触发全类型进化。 |
| `run_full_evolution` | L169-L271 |  |
| `_create_batches` | L275-L324 |  |
| `_evolve_batch` | L328-L395 |  |
| `_build_combined_prompt` | L397-L472 |  |
| `_parse_combined_response` | L474-L545 |  |
| `_parse_section` | L547-L579 |  |
| `_extract_field` | L581-L605 |  |
| `_extract_sources` | L607-L639 |  |
| `_write_evolutions` | L643-L670 |  |
| `_call_llm` | L674-L706 |  |
| `_report_progress` | L708-L718 |  |
| `_get_builtin_prompt` | L720-L770 |  |

---
## 文件：src/memory_ex/memory_ex.py

> 文件概述：实现记忆系统的完整生命周期管理，协调提取、整理、进化、检索与注入，为对话提供结构化上下文。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_load_memory_ex_config` | L28-L60 | 加载memory_ex配置并合并到全局配置 |
| `_dict_to_namespace` | L63-L69 | 递归将字典转为SimpleNamespace命名空间 |
| `_get_default_config` | L72-L134 | 返回YAML不存在时的默认配置 |

### 类列表

#### 类：`MemoryEx` (L137-L466)

> 类概述：记忆系统主类，协调存储、提取、整理等子模块

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L144-L164 |  |
| `set_llm_chat_fn` | L166-L176 | 注入LLM调用函数并分发至子模块。 |
| `set_progress_callback` | L178-L180 |  |
| `set_extract_progress_callback` | L182-L188 |  |
| `set_compaction_progress_callback` | L190-L197 |  |
| `raw_prompt_threshold` | L200-L202 |  |
| `add` | L206-L220 |  |
| `get` | L222-L227 |  |
| `search` | L229-L231 | 搜索Layer 0+Layer 2 |
| `get_working_memory` | L233-L238 |  |
| `get_context_for_query` | L240-L272 |  |
| `_build_enhanced_query` | L274-L299 |  |
| `_extract_file_paths` | L301-L331 |  |
| `_safe_read_file` | L333-L351 |  |
| `update` | L353-L355 |  |
| `delete` | L357-L359 |  |
| `clear_all` | L361-L363 |  |
| `compact` | L365-L371 | 手动触发三段式记忆整理，返回处理条目数。 |
| `compact_detailed` | L373-L378 |  |
| `stats` | L380-L427 |  |
| `maintain` | L429-L431 | 检查记忆水位并衰减评分 |
| `extract` | L435-L442 |  |
| `evolve` | L444-L446 |  |
| `check_compaction_needed` | L448-L450 |  |
| `check_evolution_needed` | L452-L454 | 调用进化器检查是否需要进化，返回布尔值。 |
| `auto_compact` | L456-L460 |  |
| `auto_evolve` | L462-L466 |  |

---
## 文件：src/memory_ex/memory_extractor.py

> 文件概述：从MD会话日志中调用LLM提取结构化记忆，含前置过滤、去重、索引规范化。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_load_prompt` | L22-L28 | 加载 Prompt 模板文件。 |

### 类列表

#### 类：`MemoryExtractor` (L31-L782)

> 类概述：从 MD 日志提取结构化记忆写入 Layer 1。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L46-L73 |  |
| `set_llm_chat_fn` | L75-L77 | 注入LLM对话函数，供记忆提取器调用。 |
| `set_progress_callback` | L79-L85 |  |
| `set_problem_base` | L87-L93 |  |
| `_find_related_problem` | L95-L127 | 为 caution 类型记忆匹配错题集记录 |
| `_read_mem_ext_record` | L129-L144 |  |
| `_migrate_old_record` | L146-L175 |  |
| `_save_mem_ext_record` | L177-L188 |  |
| `_mark_file_extracted` | L190-L199 |  |
| `_read_incremental_content` | L201-L240 |  |
| `extract_from_md_logs` | L242-L396 |  |
| `_should_skip_extraction` | L398-L415 |  |
| `_has_tech_keywords` | L417-L423 |  |
| `_extract_user_input_from_md` | L425-L434 |  |
| `_extract_content_preview` | L436-L449 |  |
| `_extract_with_llm` | L451-L502 |  |
| `_call_llm_with_timeout` | L504-L550 |  |
| `_parse_extraction_response` | L552-L657 | 解析LLM记忆提取响应，返回结构化记忆列表。 |
| `_normalize_entities` | L659-L667 |  |
| `_add_entity_context` | L669-L685 |  |
| `_write_extracted_memory` | L687-L751 |  |
| `_get_builtin_prompt` | L753-L782 |  |

---
## 文件：src/memory_ex/memory_injector.py

> 文件概述：将记忆条目格式化为带标记的注入文本，控制 token 预算并在超限时截断，统计召回条目供提示用。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_estimate_tokens` | L23-L25 | 用字符数除以2.5估算token数 |

### 类列表

#### 类：`MemoryInjector` (L28-L203)

> 类概述：将Layer1记忆格式化为带标记的注入文本

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L35-L42 |  |
| `format_for_injection` | L44-L99 | 将记忆条目格式化为带标记和计数的注入文本，超预算时截断。 |
| `_count_entries` | L101-L115 | 统计以"- "开头的行数，返回记忆条目总数 |
| `_truncate_to_budget` | L117-L196 | 按token预算截断记忆内容，优先保留进化条目。 |
| `get_injection_marker` | L198-L203 | 返回注入标记字符串，供匹配用。 |

---
## 文件：src/memory_ex/memory_interface.py

> 文件概述：定义记忆系统的抽象接口及空实现，支持存储、检索、整理、进化与自动触发，确保向后兼容。

### 类列表

#### 类：`MemoryInterface` (L13-L78)

> 类概述：定义记忆系统基础抽象接口，保持与旧接口完全兼容。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `add` | L20-L22 |  |
| `get` | L25-L27 |  |
| `search` | L30-L32 |  |
| `get_working_memory` | L35-L37 |  |
| `get_context_for_query` | L40-L48 |  |
| `update` | L51-L53 |  |
| `delete` | L56-L58 |  |
| `clear_all` | L61-L63 |  |
| `compact` | L66-L68 |  |
| `stats` | L71-L73 |  |
| `maintain` | L76-L78 |  |

#### 类：`MemoryExInterface` (L81-L123)

> 类概述：扩展接口，新增提取、进化及自动触发等方法。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `compact_detailed` | L88-L90 | 执行完整三段式整理，返回含合并/降级/ |
| `extract` | L93-L98 |  |
| `evolve` | L101-L103 |  |
| `check_compaction_needed` | L106-L108 | 检查是否需要触发记忆整理。 |
| `check_evolution_needed` | L111-L113 | 检查Layer 1积累 |
| `auto_compact` | L116-L118 |  |
| `auto_evolve` | L121-L123 |  |

#### 类：`NoopMemory` (L126-L186)

> 类概述：空实现，所有方法安全空返回，用于禁用或降级场景。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `add` | L134-L135 |  |
| `get` | L137-L138 |  |
| `search` | L140-L141 |  |
| `get_working_memory` | L143-L144 |  |
| `get_context_for_query` | L146-L147 |  |
| `update` | L149-L150 |  |
| `delete` | L152-L153 |  |
| `clear_all` | L155-L156 | 空操作清空记忆，返回空统计字典。 |
| `compact` | L158-L159 | 整理记忆，返回处理的条目数。 |
| `compact_detailed` | L161-L162 |  |
| `stats` | L164-L165 |  |
| `maintain` | L167-L168 |  |
| `extract` | L170-L171 |  |
| `evolve` | L173-L174 |  |
| `check_compaction_needed` | L176-L177 |  |
| `check_evolution_needed` | L179-L180 |  |
| `auto_compact` | L182-L183 |  |
| `auto_evolve` | L185-L186 | 自动检查并触发记忆进化 |

---
## 文件：src/memory_ex/memory_retriever.py

> 文件概述：实现Layer 1记忆的按需注入与倒排索引搜索，支持LLM预检索相关性筛选和关键词查询。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_load_prompt` | L22-L28 | 加载指定Prompt模板文件内容 |

### 类列表

#### 类：`MemoryRetriever` (L31-L419)

> 类概述：记忆召回器，支持LLM预检索与关键词搜索

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L43-L63 | 初始化配置、存储实例及LLM函数占位 |
| `set_llm_chat_fn` | L65-L67 | 注入LLM调用函数 |
| `retrieve_for_query` | L69-L136 | LLM预检索Layer 1相关记忆条目 |
| `_parse_layer1_entries` | L138-L183 | 解析MEMORY.md为结构化条目列表 |
| `_build_retrieval_prompt` | L185-L211 | 构建LLM预检索提示词 |
| `_parse_retrieval_response` | L213-L262 | 解析检索响应，提取相关条目索引 |
| `_call_llm_with_timeout` | L264-L300 | 带超时保护的LLM调用，防连接泄漏 |
| `get_layer1_content` | L302-L310 | 返回MEMORY.md全量内容 |
| `get_layer1_stats` | L312-L314 | 返回Layer 1行数与token估算 |
| `search` | L316-L365 | 倒排索引加内容匹配搜索Layer 1 |
| `search_layer0_by_keywords` | L367-L376 | 已废弃，保留接口兼容返回空列表 |
| `_extract_keywords` | L380-L397 | 分词并过滤停用词提取关键词 |
| `_matches_filters` | L399-L419 | 检查条目标签等过滤条件是否匹配 |

---
## 文件：src/memory_ex/memory_store.py

> 文件概述：管理记忆文件与元数据的读写，提供原子写入和倒排索引维护。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_atomic_write` | L20-L68 | 原子写入文件，遇锁定重试并降级为先删后写 |
| `_atomic_write_json` | L71-L74 | 将数据序列化为JSON并原子写入文件 |
| `_estimate_tokens` | L77-L79 | 按字符数除以2.5估算token数量 |
| `_count_lines` | L82-L86 | 统计文本中的行数 |

### 类列表

#### 类：`MemoryStore` (L89-L506)

> 类概述：管理Layer1和元数据的物理存储及倒排索引

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L108-L134 |  |
| `_ensure_dirs` | L138-L143 |  |
| `_ensure_files` | L145-L150 |  |
| `_cleanup_tmp_files` | L152-L159 |  |
| `_load_metadata` | L161-L170 |  |
| `read_layer1` | L174-L180 |  |
| `write_layer1` | L182-L194 | 原子写入 MEMORY.md，写入前自动备份。 |
| `get_layer1_stats` | L196-L204 | 统计 Layer 1 的记忆条数、行数和估算令牌数。 |
| `check_water_level` | L206-L225 |  |
| `save_metadata` | L229-L231 |  |
| `get_metadata_entry` | L233-L235 | 从缓存获取指定条目的元数据。 |
| `update_metadata_entry` | L237-L252 |  |
| `remove_metadata_entry` | L254-L260 |  |
| `add_compaction_log` | L262-L267 |  |
| `add_evolution_log` | L269-L274 |  |
| `add_session_checkpoint` | L276-L279 | 向元数据追加会话检查点。 |
| `update_inverted_index` | L283-L318 |  |
| `search_inverted_index` | L320-L354 |  |
| `_extract_entities` | L356-L378 |  |
| `resolve_entity` | L382-L388 |  |
| `add_entity_alias` | L390-L393 |  |
| `get_stats` | L397-L421 |  |
| `maintain` | L423-L465 |  |
| `clear_all` | L467-L489 |  |
| `get_watermarks` | L491-L498 | 返回水位配置（警告/触发/硬限制/目标）。 |
| `get_query_count_since_last_compaction` | L500-L506 |  |

---
## 文件：src/memory_ex/scoring.py

> 文件概述：实现记忆条目重要性评分，综合近度、关联度等多维指标计算分数，支持保留、降级或淘汰的层级决策。

### 类列表

#### 类：`ImportanceScorer` (L21-L203)

> 类概述：基于元数据与内容特征计算记忆重要性分数的评分器。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L30-L45 |  |
| `score` | L47-L77 |  |
| `_calc_recency` | L79-L104 |  |
| `_calc_relevance` | L106-L125 | 根据标签匹配项目核心关键词计算关联度分数。 |
| `_calc_user_explicit` | L127-L141 |  |
| `_calc_cross_module` | L143-L162 |  |
| `_calc_access_frequency` | L164-L173 |  |
| `_calc_code_absorbed` | L175-L182 |  |
| `get_score_tier` | L184-L203 | 根据重要性分数映射为保留、降级或淘汰层级。 |

---
## 文件：src/myclaude.py

> 文件概述：启动代码助手命令行工具，支持指定角色并进入交互或测试模式。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `main` | L26-L79 | 解析命令行参数并启动MyClaudeCLI交互或测试模式 |

---
## 文件：src/query/chat_llm.py

> 文件概述：封装OpenAI兼容的聊天API，支持流式对话、截断重试、token统计与多Provider适配。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_to_dict` | L11-L15 | 递归将 SimpleNamespace 转换为字典 |
| `_sanitize_messages_for_provider` | L38-L53 | 清洗消息列表，确保系统角色仅在首位 |
| `set_context` | L67-L75 | 设置查询和轮次上下文，用于 token 统计 |
| `chat_with_retry` | L78-L113 | 流式聊天，检测截断并自动翻倍重试 |
| `stream_chat` | L117-L197 | 流式调用聊天接口，返回内容与用量 |
| `simple_chat` | L200-L267 | 非流式单轮调用，异常时返回空字符串 |

---
## 文件：src/query/query_loop.py

> 文件概述：与LLM的多轮对话交互引擎，管理上下文、工具调用、记忆注入及token统计。

### 类列表

#### 类：`ChatOrNot` (L23-L26)

> 类概述：定义对话循环退出状态：继续、因无工具退出、因done退出。

#### 类：`QueryLoop` (L29-L880)

> 类概述：与LLM多轮交互引擎，仅通过回调接口通知外部。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L36-L80 |  |
| `_init_memory` | L83-L95 | 创建记忆实例并注入LLM调用，失败降级为空操作。 |
| `clear_memory` | L98-L113 |  |
| `reset_context` | L115-L121 |  |
| `new_session` | L124-L145 |  |
| `record_cli_command` | L148-L155 |  |
| `append_cli_result` | L157-L164 |  |
| `get_tokens` | L166-L171 |  |
| `run` | L174-L317 |  |
| `_references_file` | L329-L344 |  |
| `_on_llm_req` | L347-L435 |  |
| `_on_llm_rsp` | L438-L488 |  |
| `_get_follow_up_prompt` | L491-L505 |  |
| `_follow_up_for_tools` | L507-L584 |  |
| `_handle_tools` | L587-L780 |  |
| `_save_turn_memory` | L783-L822 |  |
| `_count_recalled` | L826-L836 |  |
| `_compress_assistant_message` | L840-L880 | 压缩助手消息中的工具标签，提取路径 |

---
## 文件：src/query/session_log.py

> 文件概述：提供会话日志管理与持久化，支持HTML/MD双格式输出、多轮对话跟踪及工具调用记录。 (截断)

### 类列表

#### 类：`SessionLog` (L11-L1717)

> 类概述：会话日志记录器，支持HTML/MD双格式输出，管理Query与Turn。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L13-L44 | 初始化日志目录、格式、缓冲区和去重哈希集。 |
| `init_session` | L47-L88 | 初始化会话，创建日志文件，补写待处理的CLI条目。 |
| `get_tokens` | L91-L92 | 返回请求和响应的近似token数量。 |
| `start_query` | L95-L109 | 开始新Query，关闭旧Query并立即写入标题头。 |
| `end_query` | L112-L125 | 结束当前Query，flush最后Turn并写入分隔符。 |
| `_write_query_header` | L128-L145 | 写入Query标题头到HTML和MD文件。 |
| `_write_turn_html` | L148-L238 | 将Turn缓冲按内容类型多级折叠写入HTML。 |
| `_write_turn_md` | L241-L295 | 将Turn缓冲按节写入MD文件，跳过推理内容。 |
| `_append_html_to_file` | L298-L316 | 追加HTML片段到会话日志文件末尾。 |
| `_format_md_section_content` | L319-L372 | 将小节条目按类型格式化为纯Markdown文本。 |
| `_format_md_item` | L375-L454 | 将单个日志条目格式化为纯Markdown文本。 |
| `_append_md_cli` | L457-L473 | 将CLI命令与结果写入MD文件，带子节标题。 |
| `log_turn` | L476-L482 | 开始新Turn并flush旧Turn内容。 |
| `log_llm_req` | L485-L493 | 记录LLM请求，去重消息并估算请求token。 |
| `log_llm_rsp` | L496-L503 | 记录LLM响应并估算响应token。 |
| `log_reasoning_content` | L506-L513 | 记录LLM推理内容，非空时写入折叠块。 |
| `log_tool_call` | L516-L531 | 记录工具调用，过滤冗余参数并缓存路径。 |
| `log_tool_result` | L534-L539 | 记录工具执行结果。 |
| `log_todo_snapshot` | L541-L561 | 记录当前Turn的Todo列表快照。 |
| `log_cli_command` | L564-L569 | 缓存CLI命令，等待结果后合并写入。 |
| `log_cli_result` | L571-L574 | 缓存CLI结果，与命令配对后写入日志。 |
| `_flush_cli_entry` | L576-L606 | 将CLI命令与结果合并写入HTML/MD日志。 |
| `_flush_cli_html` | L608-L672 | 将CLI命令与结果以HTML折叠节点写入。 |
| `log_dict_info` | L674-L677 | 添加时间戳和日志条目到Turn缓冲。 |
| `flush_turn` | L680-L704 | 立即将当前Turn缓冲写入HTML和MD文件。 |
| `_deduplicate_msg_list` | L707-L734 | 对API消息列表去重，只保留新增消息。 |
| `update_msg_hashes` | L736-L748 | 更新消息哈希集，避免重复记录已注入记忆。 |
| `_save_session_log` | L754-L769 | 根据格式保存会话日志条目到文件。 |
| `_parse_buffer_sections` | L772-L962 | 将Turn缓冲按内容类型分组为逻辑节。 |
| `_build_memory_section_html` | L965-L1088 | 为记忆小节构建HTML，拆分各条记忆并折叠。 |
| `_save_md` | L1091-L1101 | 将条目格式化为Markdown并追加写入日志文件。 |
| `_save_html` | L1104-L1211 | 将条目转为HTML，处理折叠和推理块，写入文件。 |
| `_html_template` | L1215-L1439 | 返回完整HTML模板，含样式、脚本及折叠交互。 |
| `_format_log_item` | L1445-L1539 | 将单个日志项格式化为Markdown文本。 |
| `_process_code_blocks` | L1542-L1570 | 识别Markdown代码块，对Python代码高亮。 |
| `_highlight_inline_python` | L1573-L1585 | 对文本内容渲染，多行Python代码则高亮。 |
| `_enhance_markdown_text` | L1588-L1644 | 对非代码块文本进行Markdown彩色渲染。 |
| `_highlight_python` | L1647-L1717 | Python语法高亮，生成带颜色样式的HTML。可保护XML标签。 |

---
## 文件：src/query/test_query.py

> 文件概述：使用 OpenAI 和 Anthropic SDK 分别调用大模型发送“hello”并获取回复。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `hello_with_openai` | L12-L26 | 通过OpenAI SDK向LLM发送'hello'并返回回复文本。 |
| `hello_with_anthropic` | L29-L50 | 通过Anthropic SDK向DeepSeek兼容接口发送'hello'并返回文本回复。 |

---
## 文件：src/query/todo_manager.py

> 文件概述：管理 TodoWrite 待办列表的解析、状态更新、进度摘要生成及生命周期，向对话上下文注入进度消息。

### 类列表

#### 类：`TodoStatus` (L13-L18)

> 类概述：待办条目状态枚举，含待办、进行中和完成。

#### 类：`TodoItem` (L22-L33)

> 类概述：单个待办项，含内容、状态与进行中动作描述。

#### 类：`TodoList` (L37-L76)

> 类概述：待办条目列表，支持整体替换和状态查询。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `is_empty` | L42-L44 |  |
| `all_completed` | L46-L50 |  |
| `current_in_progress` | L52-L57 |  |
| `completed_count` | L59-L63 |  |
| `status_signature` | L65-L76 |  |

#### 类：`TodoManager` (L79-L190)

> 类概述：管理待办列表生命周期，包括解析、状态消息和重置。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L97-L98 | 初始化空的 TodoList 实例。 |
| `todo_list` | L101-L103 |  |
| `update_from_xml` | L105-L154 |  |
| `get_context_message` | L156-L178 | 生成含进度和条目状态的 TODO 状态消息。 |
| `reset` | L180-L182 | 重置 todo 列表为空。 |
| `get_display_data` | L184-L186 |  |
| `has_todo` | L188-L190 | 返回 todo 列表是否非空。 |

---
## 文件：src/tools/ask_user_question.py

> 文件概述：实现人机回环中 LLM 向用户提问、获取答案并处理预设选项的核心交互机制。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `ask_user_question` | L13-L45 | 向用户提问并获取回答，支持预设选项或自由文本输入。 |

---
## 文件：src/tools/file_context_tool.py

> 文件概述：合并函数摘要与历史Bug记录，提供渐进式上下文披露，全程纯本地文本解析。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_find_summary_file` | L27-L33 | 在项目根目录查找函数摘要文件 |
| `_normalize_path` | L36-L38 | 将路径转为正斜杠并小写 |
| `_extract_function_summary` | L41-L90 | 从摘要文件解析提取指定文件的函数摘要 |
| `_get_bug_store` | L99-L161 | 延迟初始化BugStore单例，兼容多种构造签名 |
| `_get_field` | L167-L175 | 从对象或字典取字段值并统一返回字符串 |
| `_format_bug_record` | L178-L209 | 格式化单条Bug记录为输出文本 |
| `_score_bug_by_intent` | L238-L265 | 基于intent关键词计算Bug相关性评分 |
| `_retrieve_bugs` | L268-L303 | 检索文件相关历史Bug并按相关性排序过滤 |
| `get_file_context` | L309-L342 | 合并函数摘要与历史Bug信息，零LLM调用 |

---
## 文件：src/tools/spider.py

> 文件概述：抓取静态网页的标题与链接，解析后输出结构化JSON文件并打印内容。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `build_filename` | L23-L27 | 基于域名和时间戳生成输出文件名 |
| `fetch_html` | L30-L38 | 发送GET请求并返回响应文本 |
| `parse_content` | L41-L69 | 从HTML提取页面标题、h1-h3标题及所有链接 |
| `save_json` | L72-L78 | 将数据写入JSON文件并返回路径 |
| `crawl` | L81-L98 | 执行请求、解析、保存的完整爬取流程并返回路径 |
| `main` | L101-L121 | 解析命令行参数执行爬取，处理各类异常 |

---
## 文件：src/tools/system_test_generator_ex.py

> 文件概述：基于系统规格文档自动生成结构化系统测试用例，支持并发批量生成与验证纠错。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_find_project_root` | L38-L45 | 向上查找包含 src 和 config 目录的项目根目录 |
| `_load_global_config` | L48-L58 | 加载全局配置，失败返回 None |
| `_get_llm_client` | L61-L73 | 根据配置创建 OpenAI 客户端并返回模型名 |
| `parse_args` | L80-L97 | 解析命令行参数：规格文档路径和输出路径 |
| `_read_spec` | L104-L106 | 读取系统规格文档内容 |
| `_extract_test_scenarios` | L109-L163 | 解析文档第7章表格，提取测试场景列表 |
| `_extract_section_num` | L170-L175 | 从场景 ID 提取所属章节编号 |
| `generate_ids` | L178-L192 | 为测试用例分配格式化的唯一 ID |
| `_build_batch_prompt` | L199-L289 | 构造批量场景的 LLM 生成提示词 |
| `_extract_json` | L292-L322 | 从 LLM 响应文本中提取 JSON 数组 |
| `_call_llm` | L325-L343 | 调用 LLM 并解析返回的 JSON 列表 |
| `_assemble_cases` | L346-L368 | 将 LLM 返回条目装配为完整测试用例 |
| `_validate_single_case` | L390-L437 | 校验单个用例字段完整性与内容合规性 |
| `validate_all_cases` | L440-L458 | 分离有效用例与有错误的用例 |
| `main` | L465-L725 | 串联配置、解析、生成、验证与输出全流程 |

---
## 文件：src/tools/unit_test_generator_ex.py

> 文件概述：递归分析Python项目，结合LLM自动生成结构化JSON单元测试用例。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_load_global_config` | L28-L34 | 加载全局配置，失败返回None。 |
| `_get_llm_client` | L37-L49 | 基于全局配置创建OpenAI客户端并返回模型名。 |
| `parse_args` | L56-L73 | 解析命令行参数。 |
| `load_gitignore` | L80-L92 | 读取.gitignore，返回忽略模式列表。 |
| `should_ignore` | L95-L110 | 判断相对路径是否匹配gitignore模式。 |
| `collect_py_files` | L117-L128 | 递归收集.py文件，跳过被忽略的。 |
| `_path_to_module` | L135-L146 | 将文件路径转为Python模块导入路径。 |
| `_extract_type_hint` | L149-L159 | 从Jedi参数对象提取类型提示字符串。 |
| `analyze_file_with_jedi` | L162-L306 | 使用Jedi分析文件，提取函数签名及静态方法信息。 |
| `_module_abbr` | L313-L317 | 从模块路径生成缩写，取每段前缀大写。 |
| `_build_batch_prompt` | L324-L384 | 为批量函数构建LLM测试用例生成提示。 |
| `_extract_json` | L387-L418 | 从LLM响应中提取JSON数组。 |
| `_call_llm` | L421-L439 | 调用LLM并解析JSON响应。 |
| `_assemble_cases` | L442-L477 | 将LLM返回条目组装成完整测试用例。 |
| `_validate_single_case` | L484-L545 | 验证单个测试用例，返回错误列表。 |
| `_parse_test_input_by_params` | L548-L590 | 基于参数名列表解析test_input为字典。 |
| `_check_type_compatibility` | L593-L656 | 检查值字符串是否与类型提示兼容。 |
| `validate_all_cases` | L659-L694 | 验证所有用例，返回有效与错误用例。 |
| `_build_fix_prompt` | L701-L745 | 构建修正错误用例的LLM纠错提示。 |
| `generate_ids` | L752-L761 | 为测试用例生成唯一ID。 |
| `_find_project_root` | L764-L781 | 向上查找包含src目录的项目根。 |
| `main` | L784-L1013 | 主流程：扫描文件、分析函数、LLM生成并校验测试用例。 |

---
## 文件：src/utility/config_loader.py

> 文件概述：递归合并YAML配置并转为点号访问对象，自动加载记忆后端配置，提供全局单例。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_dict_to_namespace` | L6-L12 | 递归转换字典为支持点号访问的命名空间对象。 |
| `get_project_root` | L15-L17 | 返回项目根目录路径。 |
| `find_project_root` | L20-L39 | 从起始目录向上查找包含标记文件或特定目录名的项目根。 |
| `_deep_merge` | L42-L52 | 递归合并两字典，后者覆盖前者同名字段。 |
| `load_config` | L55-L115 | 加载并合并配置，返回点号访问命名空间，自动挂载内存后端配置。 |

---
## 文件：src/utility/excel_view.py

> 文件概述：为 MyClaude 提供 Excel 文件读取能力，将表格内容转换为带行列标题的格式化文本表格。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `view_excel` | L20-L81 | 读取 Excel 文件并返回包含行列标题的格式化文本表格。 |
| `_pick_sheet` | L84-L96 | 按名称或索引选择 openpyxl 工作表，默认返回活动表。 |
| `_pick_sheet_xlrd` | L99-L112 | 按名称或索引选择 xlrd 工作表，默认返回第一个。 |
| `_rows_to_text` | L115-L130 | 将 openpyxl 行对象转换为裁剪行列范围的文本表格。 |
| `_raw_rows_to_text` | L133-L177 | 将二维数据列表转为带行号与列标的字符串表格。 |
| `_format_cell` | L180-L187 | 将单元格值格式化为字符串，None 置空，浮点数精简显示。 |
| `_clamp` | L190-L194 | 将数值限制在指定下限与上限之间。 |

---
## 文件：src/utility/file_tool.py

> 文件概述：实现带占位符过滤、多编码容错的通用文件查看、创建、追加与精准替换操作。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_read_text_safe` | L7-L20 | 顺序尝试多种编码读取文件完整内容。 |
| `_is_invalid_path` | L31-L39 | 判断路径是否为占位符或空字符串。 |
| `add_root_path` | L42-L55 | 绝对路径直接返回，相对路径拼接根目录。 |
| `file_view` | L58-L91 | 查看文件内容片段或列出目录，支持行范围限制。 |
| `file_create` | L94-L121 | 创建新文件，若已存在且非空则阻止并提示。 |
| `file_append` | L124-L138 | 以追加模式写入内容，自动创建父目录并换行。 |
| `file_str_replace` | L141-L162 | 精确替换文件中首次出现的指定文本段。 |

---
## 文件：src/utility/llm_api_msg.py

> 文件概述：管理LLM消息序列，注入系统提示、项目上下文和目录树，支持会话重置与命令构建。

### 类列表

#### 类：`LLMAPIMessage` (L6-L237)

> 类概述：构建和管理与LLM API交互的消息序列

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L8-L34 |  |
| `_resolve_placeholders` | L37-L42 |  |
| `_load_system_prompt` | L45-L66 |  |
| `_load_project_context` | L69-L86 |  |
| `_load_tree_cache` | L90-L100 |  |
| `get_msg` | L103-L104 | 返回当前API消息列表。 |
| `reset_context` | L106-L110 | 保留系统消息，清空后续对话历史 |
| `init_api_msg` | L113-L122 |  |
| `build_command_messages` | L124-L175 | 组装斜杠命令消息，合并系统提示、指令、上下文和目录 |
| `reset_with_command` | L177-L201 |  |
| `append_micro_info` | L204-L215 |  |
| `append_llm_response` | L218-L228 |  |
| `append_tool_exec_result` | L231-L233 |  |
| `_append_info` | L236-L237 | 向内部消息列表追加消息。 |

---
## 文件：src/utility/normal_utility.py

> 文件概述：过滤LLM输出的思考标签与独立“思考过程”段落。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `strip_thinking` | L4-L16 | 移除LLM输出中的<think>标签及中文思考过程段落 |

---
## 文件：src/utility/skill_loader.py

> 文件概述：渐进式加载Skill三层实现，支持元数据缓存、完整指令及资源按需安全读取。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `get_skill_loader` | L181-L189 | 获取 SkillLoader 全局单例。 |

### 类列表

#### 类：`SkillLoader` (L8-L173)

> 类概述：渐进式加载 Skill 元数据、完整指令和资源文件，支持三层缓存。

| 方法名 | 行范围 | 方法概述 |
|--------|--------|----------|
| `__init__` | L17-L20 | 初始化加载根路径和元数据、内容缓存字典。 |
| `_scan` | L26-L56 | 扫描目录提取 SKILL.md frontmatter 元数据并缓存。 |
| `_parse_frontmatter` | L60-L88 | 解析 YAML frontmatter 为键值对，纯函数无副作用。 |
| `load_full_skill` | L94-L116 | 加载指定 Skill 完整内容并缓存，失败返回 None。 |
| `load_resource` | L122-L142 | 安全读取 Skill 目录下资源文件，返回文本或 None。 |
| `get_metadata` | L148-L150 | 返回所有 Skill 的元数据列表。 |
| `format_skills_prompt` | L153-L173 | 生成用于系统提示的 Skill 清单 Markdown。 |

---
## 文件：src/utility/token_statistics.py

> 文件概述：后台记录每次LLM调用的token消耗，生成月度明细和日/月/年汇总Excel报表。

### 函数列表

| 函数名 | 行范围 | 函数概述 |
|--------|--------|----------|
| `_get_stats_dir` | L51-L59 | 获取并缓存统计文件目录，若不存在则创建。 |
| `get_stats_dir_path` | L62-L64 | 返回统计文件目录的绝对路径字符串。 |
| `_get_monthly_detail_path` | L67-L69 | 返回当月明细XLSX文件路径。 |
| `_get_summary_path` | L72-L74 | 返回单个汇总XLSX文件（含3个sheet）的路径。 |
| `_backup_old_file` | L77-L83 | 将旧文件重命名为备份文件。 |
| `_ensure_detail_workbook` | L86-L125 | 确保月度明细文件格式正确，返回可用工作簿。 |
| `_ensure_summary_workbook` | L128-L179 | 确保汇总文件含day/month/year三个标准sheet。 |
| `_update_summary_sheet` | L182-L219 | 更新汇总sheet，按周期和模型累加token数量。 |
| `record_token_usage` | L222-L284 | 线程安全地记录单次LLM调用token消耗到明细和汇总。 |
| `get_token_summary` | L287-L333 | 从汇总day sheet读取全量累计token统计值。 |

---
