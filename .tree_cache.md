# 项目目录树 for D:\AI\MyClaude

# 生成时间：2026-06-11 07:36:21


```
MyClaude/
├── config/
│   ├── memory/
│   │   ├── memory1.yaml                                 # 配置分层记忆架构与 FAISS 
│   │   │   语义检索策略，实现智能记忆管理与遗忘机制。
│   │   └── memory2.yaml                                 # 
│   │       定义分层记忆的容量、生存时间、LLM召回、遗忘曲线与混合评分策略。
│   ├── role/
│   │   └── mycode/
│   │       ├── mycode.md                                    # 
│   │       │   定义终端AI编程助手项目的文件路径规范、架构契约、开发纪律与常见任务速查。
│   │       └── mycode_sys_prompt.md                         # 定义MyClaude 
│   │           Code的工作规则、编码任务触发条件、工具使用规范与任务终止流程。
│   ├── config.yaml                                  # 
│   │   全局配置，定义模型参数、工具解析、记忆后端、目录结构、CLI界面和日志格式等核心设置。
│   └── model_key.yaml                               # 
│       集中管理多AI模型API密钥与连接参数，并为记忆模块提供嵌入与LLM压缩配置。
├── skill/
│   └── add_tests/
│       └── SKILL.md                                     # 为 Python 代码自动生成 pytest 
│           单元测试，分析源码创建测试文件，运行并修正测试，覆盖正常、边界与异常路径。
├── src/
│   ├── A2A/
│   │   ├── myorch/
│   │   │   ├── agent_card.py                                # 定义 MyOrch 验证编排服务的 A2A Agent 身份、技能与端点。
│   │   │   ├── context_store.py                             # 持久化进化验证任务的元数据、测试用例和报告，支持按 
│   │   │   │   task_id 查询历史记录与全局指标统计。
│   │   │   ├── main.py                                      # A2A 验证编排服务入口，提供任务提交、状态查询、健康检查及 
│   │   │   │   Prometheus 指标接口。
│   │   │   ├── models.py                                    # 定义 MyOrch 服务的验证任务、健康检查与监控指标 Pydantic 
│   │   │   │   模型，扩展共享请求/响应结构。
│   │   │   └── orchestrator.py                              # 协调SystemTest 
│   │   │       Agent执行回归与新功能测试，根据通过率阈值判定验证结果，返回PASS/FAIL/ERROR。
│   │   ├── shared/
│   │   │   ├── config.py                                    # 
│   │   │   │   从环境变量加载A2A服务、Docker、LLM等参数，生成统一配置命名空间供全局使用。
│   │   │   └── models.py                                    # 定义验证任务、测试用例及报告的Pydantic数据模型与枚举。
│   │   ├── test/
│   │   │   ├── agent_card.py                                # 定义系统测试服务的A2A 
│   │   │   │   Agent身份、技能与能力端点，用于提供回归和新功能测试。
│   │   │   ├── judge.py                                     # 
│   │   │   │   调用大语言模型对比测试用例的实际输出与期望行为，判定通过、失败或无法确定。
│   │   │   ├── main.py                                      # 提供系统测试执行器REST 
│   │   │   │   API，支持回归测试、新功能测试与单元测试。
│   │   │   ├── models.py                                    # 定义SystemTest服务的测试请求/响应数据结构。
│   │   │   ├── new_feature_runner.py                        # 
│   │   │   │   在沙箱中执行新功能验收用例，调用评判LLM输出PASS/FAIL结果。
│   │   │   ├── regression_runner.py                         # 
│   │   │   │   在Docker沙箱中运行8项内置回归测试，验证MyClaude核心能力，并用LLM判定通过与否。
│   │   │   ├── sandbox.py                                   # 为 MyClaude 提供 Docker 
│   │   │   │   容器隔离运行环境，支持命令执行与测试输出，Docker 不可用时自动降级为本地子进程。
│   │   │   └── unit_test_runner.py                          # 
│   │   │       通过动态导入调用被测函数，利用LLMJudge评判返回值并生成Excel测试报告。
│   │   └── requirements.txt                             # 
│   │       构建基于FastAPI的异步微服务，集成A2A通信、Docker交互与Prometheus监控能力。
│   ├── cli/
│   │   ├── cli_print.py                                 # 实现MyClaude CLI的界面打印、HTML会话记录与打字机效果渲染。
│   │   ├── code_statistics.py                           # 
│   │   │   统计项目代码行数，按目录展示文件详情并按扩展名汇总，支持解析.gitignore跳过指定目录，通过CLI命令调用。
│   │   ├── h2m.py                                       # 
│   │   │   将MyClaude会话日志HTML转换为纯净Markdown，支持按轮次和小节筛选内容。
│   │   ├── mycli.py                                     # 
│   │   │   整合CLI交互、会话管理与测试模式，提供命令解析、对话循环、工具调用捕获及JSON结果输出。
│   │   └── tree_visualizer.py                           # 递归扫描项目目录并调用LLM为文件生成摘要，输出可视化项目树。
│   ├── llm_tool/
│   │   ├── cmd_bash.py                                  # 
│   │   │   执行经过安全过滤的Shell命令，拦截无效占位符与受编码/执行策略影响的文件内容搜索。
│   │   ├── re_test.py                                   # 用正则从响应文本中解析并提取 `<str_replace>` 
│   │   │   标签的属性与内容。
│   │   └── tool_executor.py                             # 解析 AI 响应中的 XML 
│   │       工具调用，支持嵌套感知与标签清理，将指令转为可执行的操作参数。
│   ├── memory/
│   │   ├── memory_1/
│   │   │   ├── adapter.py                                   # 
│   │   │   │   将Memory1Backend封装为统一记忆接口，注入压缩与注入器并构建嵌入/LLM回调，供query_loop使用。
│   │   │   ├── memory1.py                                   # 
│   │   │   │   实现三层记忆系统，提供记添加、语义检索、压缩与遗忘等记忆管理功能。
│   │   │   ├── memory_compressor.py                         # 
│   │   │   │   当短期记忆超量时，调用LLM将多条对话记录压缩为一条保留主题、决策与结论的长期摘要。
│   │   │   ├── memory_injector.py                           # 将工作记忆与检索记忆格式化为 Markdown 上下文，并按 token 
│   │   │   │   预算截断后注入 LLM。
│   │   │   ├── memory_retriever.py                          # 
│   │   │   │   实现基于FAISS的语义检索及混合打分，支持向量与关键词双模式检索。
│   │   │   └── memory_store.py                              # 
│   │   │       基于JSON的记忆持久化存储，支持原子写入、滚动备份、损坏恢复及增删改查。
│   │   ├── memory_2/
│   │   │   ├── adapter.py                                   # 封装 Memory2Backend 为 MemoryInterface 
│   │   │   │   适配器，集成注入与压缩，向 query_loop 提供会话日志兼容的记忆上下文。
│   │   │   ├── memory2.py                                   # 
│   │   │   │   实现三层记忆系统（工作/短期/长期），通过LLM直接召回检索，无向量索引，集成记忆存储、压缩与注入。
│   │   │   ├── memory_compressor.py                         # 
│   │   │   │   将多条对话记忆通过LLM合并为一条精炼摘要，保留决策与关键信息，丢弃冗余。
│   │   │   ├── memory_injector.py                           # 
│   │   │   │   将工作记忆与检索到的记忆格式化为LLM可用的Markdown上下文，并按token预算截断。
│   │   │   ├── memory_retriever.py                          # 
│   │   │   │   不依赖向量嵌入，直接交给LLM对记忆条目进行相关性评分与精排，实现上下文相关记忆召回。
│   │   │   ├── memory_store.py                              # 
│   │   │   │   基于JSON的记忆持久化存储层，提供原子写入、滚动备份、损坏恢复及标签时间窗口过滤的CRUD操作。
│   │   │   └── retrieval_prompt.md                          # 
│   │   │       基于查询与记忆的四维度相关性评分，并优先拦截无实质内容查询。
│   │   ├── factory.py                                   # 
│   │   │   根据配置动态创建记忆后端实例，支持适配器优先和回退加载，解耦调用模块。
│   │   └── memory_interface.py                          # 
│   │       定义记忆子系统抽象接口，规范添加、检索、压缩、统计等操作，并提供空操作实现。
│   ├── query/
│   │   ├── chat_llm.py                                  # 
│   │   │   封装OpenAI流式聊天，支持截断自动翻倍重试、用量累加与异常处理。
│   │   ├── query_loop.py                                # 多轮LLM交互循环，处理工具调用、记忆管理与精确Token统计。
│   │   ├── session_log.py                               # 实现会话日志记录，支持多轮对话、请求/响应统计及 HTML 
│   │   │   多级折叠展示。
│   │   └── test_query.py                                # 测试OpenAI与Anthropic SDK向LLM发送问候并获取回复的调用功能。
│   ├── tools/
│   │   └── spider.py                                    # 抓取静态网页的标题和链接，输出结构化JSON文件。
│   ├── utility/
│   │   ├── config_loader.py                             # 
│   │   │   递归合并YAML配置，转为点号可访问的命名空间对象，并自动加载记忆模块配置。
│   │   ├── file_tool.py                                 # 
│   │   │   提供安全的文件查看、创建、追加和精确替换功能，自动处理编码兼容与非法占位符路径拦截。
│   │   ├── llm_api_msg.py                               # 构建与 LLM 
│   │   │   交互的消息序列，注入系统提示、项目上下文与目录树缓存，支持追加对话与工具结果。
│   │   ├── normal_utility.py                            # 去除LLM输出中的`<think>`标签及独立思考段落，防止内容污染。
│   │   └── skill_loader.py                              # 实现 Skill 
│   │       三层渐进式加载（L1元数据/L2完整指令/L3资源），提供单例访问。
│   └── myclaude.py                                  # 作为程序入口，解析命令行参数并启动 MyClaude 交互模式或测试模式。
├── AGENTS.md                                    # 描述MyClaude项目的启动、配置、架构、工具协议和关键行为规则。
├── LICENSE                                      # 
│   定义开源软件的使用、修改与分发条款，包含专利授权、免责声明及责任限制。
├── pip_install.bat                              # 
│   批量安装项目所需的OpenAI、Rich、Prompt-Toolkit及PyYAML等Python依赖包。
├── pytest.ini                                   # 定义pytest测试发现规则与运行参数，指定源代码和输出目录。
├── README.md                                    # 介绍项目目标：仿制 Claude Code，并期望从 0.01 版本自我进化到 0.1 
│   版本。
├── ruff.toml                                    # 配置 Ruff 
│   代码检查工具，选择基础错误、导入排序、代码简化等规则集，忽略文档字符串等过严检查项。
├── start.bat                                    # 配置网络代理并禁用TLS验证，启动myclaude应用。
└── testdata.txt                                 # 测试用示例数据
```
