# 项目目录树 for D:\AI\MyClaude

# 生成时间：2026-06-11 07:18:28


```
MyClaude/
├── config/
│   ├── memory/
│   │   ├── memory1.yaml                                 # 
│   │   │   定义分层记忆架构、Embedding向量检索与混合打分遗忘策略的AI对话记忆配置。
│   │   └── memory2.yaml                                 # 定义分层记忆系统的容量、召回、遗忘与打分等参数。
│   ├── role/
│   │   └── mycode/
│   │       ├── mycode.md                                    # 定义项目路径规范、架构契约与开发纪律，为 AI 
│   │       │   编程助手提供完整的编码与行为规则。
│   │       └── mycode_sys_prompt.md                         # 定义 AI 
│   │           编程助手的行为规则，实现代码生成、文件操作、错误恢复与任务终止的严格约束。
│   ├── config.yaml                                  # 定义模型交互、工具解析、记忆系统、项目路径、CLI 
│   │   及日志等全局运行参数。
│   └── model_key.yaml                               # 
│       集中管理多个大模型API密钥、接口地址及模型参数，并为记忆模块指定嵌入与压缩模型。
├── skill/
│   └── add_tests/
│       └── SKILL.md                                     # 自动分析源码生成 pytest 单元测试，运行并修正测试代码。
├── src/
│   ├── A2A_EX/
│   │   ├── myorch/
│   │   │   ├── agent_card.py                                # 
│   │   │   │   定义MyOrch验证编排服务的A2A身份、技能与端点，用于协调系统测试任务。
│   │   │   ├── context_store.py                             # 
│   │   │   │   持久化进化验证任务的元数据、测试用例和报告，支持按task_id查询历史记录和全局指标统计。
│   │   │   ├── main.py                                      # A2A 验证编排服务入口，提供验证任务提交与查询、健康检查和 
│   │   │   │   Prometheus 监控端点。
│   │   │   ├── models.py                                    # 为 MyOrch 服务定义验证任务状态、健康检查及指标响应的 
│   │   │   │   Pydantic 数据模型。
│   │   │   └── orchestrator.py                              # 协调SystemTest 
│   │   │       Agent执行回归与新功能测试，根据阈值判定并返回PASS/FAIL/ERROR结果。
│   │   ├── shared/
│   │   │   ├── config.py                                    # 
│   │   │   │   从环境变量加载服务、Docker及LLM评判等配置，生成统一全局配置命名空间。
│   │   │   └── models.py                                    # 
│   │   │       定义验证系统所需的共享Pydantic数据模型，涵盖进化需求、测试用例及验证报告等结构。
│   │   ├── test/
│   │   │   ├── agent_card.py                                # 
│   │   │   │   定义系统测试服务的A2A代理卡片，包含回归与新功能测试技能及端点配置。
│   │   │   ├── judge.py                                     # 
│   │   │   │   基于LLM比对测试实际输出与期望行为，自动判定PASS/FAIL/INCONCLUSIVE并给出理由。
│   │   │   ├── main.py                                      # 提供系统测试执行的REST 
│   │   │   │   API，支持回归测试、新功能测试和单元测试，并包含A2A Agent Card发现端点。
│   │   │   ├── models.py                                    # 
│   │   │   │   定义系统测试服务的请求、响应与结果数据模型，涵盖回归、新功能及单元测试场景。
│   │   │   ├── new_feature_runner.py                        # 
│   │   │   │   在沙箱中执行新功能验收用例，调用评判LLM判定通过或失败，返回测试结果。
│   │   │   ├── regression_runner.py                         # 在沙箱中执行 MyClaude 内置回归测试套件，并借助 LLM 
│   │   │   │   评判各用例通过与否。
│   │   │   ├── sandbox.py                                   # 
│   │   │   │   封装Docker沙箱，为MyClaude提供隔离命令执行环境，并在Docker不可用时降级为本地子进程。
│   │   │   └── unit_test_runner.py                          # 
│   │   │       通过动态导入被测模块并调用其函数，由LLMJudge评判结果，生成Excel测试报告。
│   │   └── requirements.txt                             # 
│   │       定义了基于FastAPI构建A2A服务、集成Docker交互与Prometheus监控所需的核心依赖。
│   ├── cli/
│   │   ├── cli_print.py                                 # 提供基于 Rich 的 CLI 
│   │   │   交互界面，负责打印用户输入、助手回复、工具调用及错误信息，并支持 Markdown 渲染、打字机效果与对话历史 HTML 
│   │   │   保存。
│   │   ├── code_statistics.py                           # 
│   │   │   按目录和扩展名统计项目代码行数（含注释/空行），支持解析.gitignore跳过忽略目录，并以表格输出结果。
│   │   ├── h2m.py                                       # 将 MyClaude 会话日志 HTML 转换为 
│   │   │   Markdown，支持按轮次和小节筛选。
│   │   ├── mycli.py                                     # 
│   │   │   基于查询循环的交互式命令行界面，支持对话、指令操控和测试模式。
│   │   └── tree_visualizer.py                           # 递归扫描项目目录，调用LLM生成文件概述并输出带注释的树形结构。
│   ├── llm_tool/
│   │   ├── cmd_bash.py                                  # 封装安全的 shell 命令执行，拦截 LLM 
│   │   │   占位符和受限的文件内容搜索命令。
│   │   ├── re_test.py                                   # 从XML响应中提取路径与替换内容的str_replace标签解析功能测试。
│   │   └── tool_executor.py                             # 
│   │       解析AI响应中的XML工具调用，支持嵌套感知与容错清理，提取文件操作与命令执行指令。
│   ├── memory/
│   │   ├── memory_1/
│   │   │   ├── adapter.py                                   # 将 Memory1Backend 适配为统一记忆接口，为 query_loop 
│   │   │   │   注入压缩器与嵌入模型，提供记忆增删检索及上下文注入功能。
│   │   │   ├── memory1.py                                   # 
│   │   │   │   实现三层记忆系统，通过嵌入与FAISS向量检索混合打分，管理上下文并支持压缩。
│   │   │   ├── memory_compressor.py                         # 
│   │   │   │   当短期记忆超量时调用LLM将多条记忆压缩为一条保留关键信息的长期摘要。
│   │   │   ├── memory_injector.py                           # 
│   │   │   │   将工作记忆与检索记忆整合为Markdown上下文，按token预算智能截断，供LLM直接注入。
│   │   │   ├── memory_retriever.py                          # 实现基于 Embedding + FAISS 
│   │   │   │   的语义检索，结合时间衰减与重要性进行多因子混合打分排序。
│   │   │   └── memory_store.py                              # 实现基于 JSON 的记忆条目持久化存储，支持 
│   │   │       CRUD、原子写入、滚动备份和损坏恢复。
│   │   ├── memory_2/
│   │   │   ├── adapter.py                                   # 
│   │   │   │   将Memory2Backend适配为MemoryInterface，组装注入器与压缩器，向query_loop提供统一记忆操作接口。
│   │   │   ├── memory2.py                                   # 
│   │   │   │   实现基于LLM直接召回的三层记忆系统，管理工作、短期与长期记忆，支持压缩、注入与检索。
│   │   │   ├── memory_compressor.py                         # 将多条短期记忆通过LLM分批压缩为精炼长期摘要，去冗存关键。
│   │   │   ├── memory_injector.py                           # 
│   │   │   │   格式化工作记忆与检索结果为LLM可注入的Markdown上下文，按token预算截断并去重合并。
│   │   │   ├── memory_retriever.py                          # 
│   │   │   │   直接调用大模型对候选记忆进行相关性评分，实现无需向量的语义检索，支持分批、缓存与多维度校准。
│   │   │   ├── memory_store.py                              # 
│   │   │   │   纯文本JSON记忆持久化存储，支持原子写入、滚动备份、损坏恢复、CRUD及预过滤查询，严格禁止向量字段。
│   │   │   └── retrieval_prompt.md                          # 基于四维度和防闲聊规则，对编程记忆与查询进行相关性评分。
│   │   ├── factory.py                                   # 
│   │   │   根据配置动态创建对应记忆后端实例，支持适配器与回退机制，封装多种实现加载。
│   │   └── memory_interface.py                          # 
│   │       定义记忆子系统的统一抽象接口与空实现，约束增删改查、检索、压缩及维护等方法。
│   ├── query/
│   │   ├── chat_llm.py                                  # 
│   │   │   封装OpenAI流式聊天接口，支持自动扩容重试截断回复，处理异常与用量统计。
│   │   ├── query_loop.py                                # 
│   │   │   实现与LLM多轮对话、工具执行、记忆召回和token统计的核心交互引擎。
│   │   ├── session_log.py                               # 实现对话会话日志的记录、去重与HTML/Markdown多级折叠展示。
│   │   └── test_query.py                                # 验证通过OpenAI及Anthropic兼容接口与LLM的“hello”对话功能。
│   ├── tools/
│   │   └── spider.py                                    # 
│   │       抓取网页标题及超链接，解析为结构化JSON保存并输出，支持命令行调用。
│   ├── utility/
│   │   ├── config_loader.py                             # 
│   │   │   加载并合并YAML配置文件，支持记忆配置自动挂载，返回可通过点号访问的命名空间对象。
│   │   ├── file_tool.py                                 # 
│   │   │   安全文件操作工具，支持多编码读取、路径校验与创建/追加/替换，防止LLM输出无效路径。
│   │   ├── llm_api_msg.py                               # 
│   │   │   构建LLM对话消息上下文，整合系统提示、项目说明和目录树，并管理用户请求与助手回复的累积。
│   │   ├── normal_utility.py                            # 过滤LLM输出中的<think>标签及思考过程行，去除思考内容。
│   │   └── skill_loader.py                              # 
│   │       实现三层渐进式Skill加载（元数据、完整指令、资源），带缓存和路径安全。
│   └── myclaude.py                                  # MyClaude Code CLI 
│       入口，配置日志、解析角色与测试模式参数并启动交互或测试运行。
├── AGENTS.md                                    # 
│   定义MyClaude项目的启动方式、配置结构、LLM工具协议、核心规则与路径规范，是代理的完整操作手册。
├── LICENSE                                      # 声明项目采用Apache 
│   2.0许可，规定使用、复制、分发及衍生作品的授权与限制条件。
├── pip_install.bat                              # 批量安装项目所需的 openai、rich、prompt-toolkit 与 PyYAML 依赖包。
├── pytest.ini                                   # 配置pytest测试发现路径、命名规则、默认参数与忽略目录。
├── README.md                                    # 搭建 Claude Code 仿制项目，构建0.01版本并使其自我进化至0.1版本。
├── ruff.toml                                    # 配置 Ruff 代码检查器的规则选择、忽略项与排除目录。
├── start.bat                                    # 配置HTTP代理并启动myclaude模块。
└── testdata.txt                                 # 辅助模块
```
